version: "2"

linters:
  enable:
    - govet
    - staticcheck
    - ineffassign
  disable:
    - errcheck
    - gosec
    - dupl
    - gocyclo

run:
  timeout: 5m
  skip-dirs:
    - test
  skip-files:
    - _test\.go
