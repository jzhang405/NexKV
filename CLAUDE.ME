# 【Pre 文档】基于KV映射的元数据管理架构

> **分支**: feature/metadata-kv-implementation
> **PR编号**: PR-MetadataKV
> **负责人**: 🤖 核心开发 A
> **创建日期**: 2025-02-10
> **更新日期**: 2025-02-10（补充元数据映射说明）

---

## 1. 背景与目标

### 1.1 背景

NexKV 当前元数据管理存在以下问题：
- **命名冲突**：不同类型元数据使用相同键前缀，容易冲突
- **类型不安全**：使用 `map[string]string` 存储，需要类型断言
- **版本控制缺失**：元数据层没有封装 MVCC 版本控制接口
- **代码重复**：HostManager、TreeCoordinator 有相似代码
- **一致性策略不清晰**：未区分不同元数据的一致性要求

### 1.2 核心目标

1. **功能目标**：
   - 实现 9 个命名空间隔离（Cluster、Node、Role、Topo、Shard、Static、Dynamic、Op、Version）
   - 实现 6 种强类型元数据结构
   - 提供统一的 MetadataKV 封装层和高层 API
   - **明确不同元数据的更新频率和一致性要求**

2. **性能目标**：
   - 点查询延迟 < 1ms（P99）
   - 批量查询吞吐 > 100K ops/s

3. **质量目标**：
   - 单元测试覆盖率 ≥ 85%
   - Code Review 无 P0/P1 问题

### 1.3 明确边界

**本次不支持**：
- 不修改 MVStore 底层实现
- 不实现新的 Gossip 协议
- 不实现跨数据中心元数据同步

---

## 2. 元数据 KV 映射设计

### 2.1 元数据分类矩阵

| 命名空间 | 元数据类型 | 更新频率 | 一致性要求 | 同步机制 | 键格式示例 |
|---------|-----------|---------|-----------|---------|-----------|
| **NamespaceCluster** | 集群配置 | 极低 | 强一致 | Quorum | `meta:cluster:config` |
| **NamespaceNode** | 节点信息 | 低 | 最终一致 | Gossip | `meta:node:{node_id}` |
| **NamespaceRole** | 角色信息 | 低 | 最终一致 | Gossip | `meta:role:{role_id}` |
| **NamespaceTopo** | 拓扑关系 | 中 | 最终一致 | Gossip | `meta:topo:{node_id}` |
| **NamespaceShard** | 分片信息 | 中 | 强一致 | Quorum | `meta:shard:{shard_id}` |
| **NamespaceStatic** | 静态配置 | 极低 | 强一致 | Quorum | `meta:static:max_children` |
| **NamespaceDynamic** | 动态状态 | 高 | 最终一致 | Gossip | `meta:dynamic:{node_id}:cpu` |
| **NamespaceOp** | 操作记录 | 高 | 最终一致 | Gossip | `meta:op:{op_id}` |
| **NamespaceVersion** | 版本控制 | 高 | 强一致 | Quorum | `meta:version:{key}:{ver}` |

### 2.2 更新频率定义

| 频率级别 | 定义 | 典型场景 |
|---------|------|---------|
| **极低** | 每天几次 → 每月几次 | 集群初始化、配置变更 |
| **低** | 每小时几次 → 每天几次 | 节点上下线、角色变更 |
| **中** | 每分钟几次 → 每小时几次 | 分片迁移、拓扑调整 |
| **高** | 每秒几次 → 每分钟几次 | 心跳更新、负载统计 |

### 2.3 一致性要求定义

| 一致性级别 | 定义 | 同步机制 | 典型场景 |
|-----------|------|---------|---------|
| **强一致** | 写入后立即对所有节点可见 | Quorum（多数派确认） | 分片创建、配置变更 |
| **最终一致** | 写入后异步扩散，秒级一致 | Gossip（随机选点） | 节点状态、负载信息 |

### 2.4 详细元数据映射

#### 📍 NamespaceCluster - 集群级别元数据

| 键模式 | 元数据类型 | 更新频率 | 一致性 | 说明 |
|--------|-----------|---------|--------|------|
| `meta:cluster:config` | ClusterInfo | 极低 | 强一致 | 集群配置（ID、名称、版本） |
| `meta:cluster:state` | ClusterState | 低 | 强一致 | 集群状态（Initializing/Running） |
| `meta:cluster:quorum` | QuorumConfig | 极低 | 强一致 | Quorum 配置（阈值、超时） |

**示例**：
```
Key:   meta:cluster:config
Value: {
  ClusterID: "cluster-001",
  ClusterName: "prod-cluster",
  ClusterVersion: "1.0.0",
  State: "Running",
  QuorumThreshold: 3,
  GossipInterval: 10000000000,  // 10s
  CreatedAt: "2025-02-10T00:00:00Z",
  UpdatedAt: "2025-02-10T00:00:00Z",
  Version: 1234567890
}
```

#### 📍 NamespaceNode、NamespaceRole、NamespaceTopo 三者关系

```mermaid
graph TB
    subgraph "NamespaceNode: 节点基本信息"
        N1["node-001<br/>NodeID: node-001<br/>HostID: host-001<br/>Role: Leaf<br/>Addr: 192.168.1.10:9000<br/>Status: Ready"]
        N2["node-002<br/>NodeID: node-002<br/>HostID: host-002<br/>Role: Leaf<br/>Addr: 192.168.1.11:9000<br/>Status: Ready"]
        N3["node-003<br/>NodeID: node-003<br/>HostID: host-003<br/>Role: Parent<br/>Addr: 192.168.1.12:9000<br/>Status: Ready"]
    end

    subgraph "NamespaceRole: 角色与成员关系"
        R1["role-parent-001<br/>RoleType: Parent<br/>ActiveNodes: [node-003, node-004]<br/>StandbyNodes: [node-005]<br/>CurrentPrimary: node-003"]
        R2["role-leaf-001<br/>RoleType: Leaf<br/>ActiveNodes: [node-001, node-002]<br/>StandbyNodes: []<br/>CurrentPrimary: N/A"]
    end

    subgraph "NamespaceTopo: 拓扑层级关系"
        T1["node-003<br/>ParentID: (root)<br/>ChildrenIDs: [node-001, node-002]<br/>Level: 0"]
        T2["node-001<br/>ParentID: node-003<br/>ChildrenIDs: []<br/>Level: 1"]
        T3["node-002<br/>ParentID: node-003<br/>ChildrenIDs: []<br/>Level: 1"]
    end

    %% 关系连线
    N1 -.->|属于| R2
    N2 -.->|属于| R2
    N3 -.->|属于| R1

    N1 -->|对应| T2
    N2 -->|对应| T3
    N3 -->|对应| T1

    T1 -->|父节点| T2
    T1 -->|父节点| T3

    style N1 fill:#e1f5ff
    style N2 fill:#e1f5ff
    style N3 fill:#e1f5ff
    style R1 fill:#fff3e0
    style R2 fill:#fff3e0
    style T1 fill:#f3e5f5
    style T2 fill:#f3e5f5
    style T3 fill:#f3e5f5
```

**关系说明**：

| 关系类型 | 说明 | 示例 |
|---------|------|------|
| **Node → Role** | 一个节点属于某个角色（Active 或 Standby） | node-001 属于 role-leaf-001 |
| **Node → Topo** | 一个节点有拓扑关系（父/子节点） | node-001 的父节点是 node-003 |
| **Role → Topo** | 角色类型决定节点在拓扑中的层级 | Parent 角色在 Level 0，Leaf 在 Level 1 |

**数据一致性流程**：

```mermaid
sequenceDiagram
    participant Client
    participant NodeAPI
    participant RoleAPI
    participant TopoAPI
    participant MVStore

    Client->>NodeAPI: 更新节点状态 (node-001)
    NodeAPI->>MVStore: Put(meta:node:node-001, ...)
    MVStore-->>NodeAPI: OK

    Note over NodeAPI,RoleAPI: 节点角色变更时

    Client->>RoleAPI: 节点加入角色 (node-001 → role-leaf-001)
    RoleAPI->>MVStore: Put(meta:role:role-leaf-001, ...)
    MVStore-->>RoleAPI: OK

    Note over RoleAPI,TopoAPI: 拓扑关系变更时

    Client->>TopoAPI: 设置父节点 (node-001 → node-003)
    TopoAPI->>MVStore: Put(meta:topo:node-001, ...)
    MVStore-->>TopoAPI: OK

    Note over MVStore: Gossip 异步扩散到所有节点
```

#### 📍 NamespaceNode - 节点元数据

| 键模式 | 元数据类型 | 更新频率 | 一致性 | 说明 |
|--------|-----------|---------|--------|------|
| `meta:node:{node_id}` | NodeInfo | 低 | 最终一致 | 节点基本信息 |
| `meta:node:{node_id}:status` | NodeStatus | 高 | 最终一致 | 节点状态（心跳更新） |
| `meta:node:{node_id}:capacity` | NodeCapacity | 中 | 最终一致 | 节点容量信息 |

**示例**：
```
Key:   meta:node:node-001
Value: {
  NodeID: "node-001",
  HostID: "host-001",
  Role: "Leaf",
  Addr: { Host: "192.168.1.10", TCPPort: 9000 },
  ParentID: "parent-001",
  Level: 2,
  Status: "Ready",
  Priority: 0,
  LastHeartbeat: "2025-02-10T12:34:56Z",
  Version: 1234567890
}
```

#### 📍 NamespaceRole - 角色元数据（含 Standby）

| 键模式 | 元数据类型 | 更新频率 | 一致性 | 说明 |
|--------|-----------|---------|--------|------|
| `meta:role:{role_id}` | RoleInfo | 低 | 最终一致 | 角色定义和成员列表 |
| `meta:role:{role_id}:primary` | PrimaryInfo | 低 | 强一致 | 当前 Primary 节点 |
| `meta:role:{role_id}:failover` | FailoverHistory | 低 | 最终一致 | 故障转移历史（最近 100 条） |

**示例**：
```
Key:   meta:role:role-parent-001
Value: {
  RoleID: "role-parent-001",
  RoleType: "Parent",
  ActiveNodes: ["node-001", "node-002"],
  StandbyNodes: ["node-003", "node-004"],
  CurrentPrimary: "node-001",
  LastFailover: "2025-02-10T10:00:00Z",
  FailoverHistory: [
    { From: "node-005", To: "node-001", Time: "...", Reason: "heartbeat_timeout" }
  ],
  Version: 1234567890
}
```

#### 📍 NamespaceTopo - 拓扑元数据

| 键模式 | 元数据类型 | 更新频率 | 一致性 | 说明 |
|--------|-----------|---------|--------|------|
| `meta:topo:{node_id}` | TopologyInfo | 中 | 最终一致 | 节点拓扑关系 |
| `meta:topo:tree:{level}` | TreeLevelInfo | 中 | 最终一致 | 层级拓扑信息 |

**示例**：
```
Key:   meta:topo:node-001
Value: {
  NodeID: "node-001",
  ParentID: "parent-001",
  ChildrenIDs: ["child-001", "child-002"],
  Level: 2,
  Version: 1234567890
}
```

#### 📍 NamespaceShard - 分片元数据

| 键模式 | 元数据类型 | 更新频率 | 一致性 | 说明 |
|--------|-----------|---------|--------|------|
| `meta:shard:{shard_id}` | ShardInfo | 中 | 强一致 | 分片配置和状态 |
| `meta:shard:{shard_id}:replicas` | ReplicaList | 中 | 强一致 | 副本分布 |
| `meta:shard:{shard_id}:migration` | MigrationState | 低 | 强一致 | 数据迁移状态 |

**示例**：
```
Key:   meta:shard:shard-001
Value: {
  ShardID: "shard-001",
  RangeStart: "key-00000",
  RangeEnd: "key-10000",
  ReplicaNodes: ["node-001", "node-002", "node-003"],
  State: "Active",
  Version: 1234567890
}
```

#### 📍 NamespaceStatic - 静态配置元数据

| 键模式 | 元数据类型 | 更新频率 | 一致性 | 说明 |
|--------|-----------|---------|--------|------|
| `meta:static:max_children` | int | 极低 | 强一致 | 最大子节点数 |
| `meta:static:max_level` | int | 极低 | 强一致 | 最大树深度 |
| `meta:static:heartbeat_interval` | duration | 极低 | 强一致 | 心跳间隔 |
| `meta:static:heartbeat_timeout` | duration | 极低 | 强一致 | 心跳超时 |

**示例**：
```
Key:   meta:static:max_children
Value: 10  // MessagePack 编码的 int
```

#### 📍 NamespaceDynamic - 动态状态元数据

| 键模式 | 元数据类型 | 更新频率 | 一致性 | 说明 |
|--------|-----------|---------|--------|------|
| `meta:dynamic:{node_id}:cpu` | float | 高 | 最终一致 | CPU 使用率 |
| `meta:dynamic:{node_id}:memory` | float | 高 | 最终一致 | 内存使用率 |
| `meta:dynamic:{node_id}:throughput` | int | 高 | 最终一致 | 吞吐量 |
| `meta:dynamic:{node_id}:latency` | duration | 中 | 最终一致 | 延迟数据 |

**示例**：
```
Key:   meta:dynamic:node-001:cpu
Value: 45.5  // MessagePack 编码的 float
```

#### 📍 NamespaceOp - 操作记录元数据

| 键模式 | 元数据类型 | 更新频率 | 一致性 | 说明 |
|--------|-----------|---------|--------|------|
| `meta:op:{op_id}` | OperationInfo | 高 | 最终一致 | 操作信息 |
| `meta:op:{op_id}:progress` | OperationProgress | 高 | 最终一致 | 操作进度 |

**示例**：
```
Key:   meta:op:op-20250210-001
Value: {
  OpID: "op-20250210-001",
  OpType: "ShardMigration",
  ShardID: "shard-001",
  Status: "InProgress",
  Progress: 45,
  StartTime: "2025-02-10T12:00:00Z",
  Version: 1234567890
}
```

#### 📍 NamespaceVersion - 版本控制元数据

| 键模式 | 元数据类型 | 更新频率 | 一致性 | 说明 |
|--------|-----------|---------|--------|------|
| `meta:version:{key}:latest` | uint64 | 高 | 强一致 | 最新版本号 |
| `meta:version:{key}:{ver}` | VersionInfo | 高 | 强一致 | 版本详细信息 |

**示例**：
```
Key:   meta:version:node-001:latest
Value: 1234567890  // MessagePack 编码的 uint64
```

---

## 3. 实现方案

### 3.1 整体架构

```
API 层（MetadataAPI、ClusterAPI、NodeAPI、ShardAPI）
    ↓
类型层（ClusterInfo、NodeInfo、RoleInfo 等 6 种类型）
    ↓
KV 封装层（MetadataKV + MetadataCodec + 错误处理）
    ↓
存储层（MVStore + WAL）
```

### 3.2 一致性机制集成

**写入接口扩展**：
```go
// PutOptions 写入选项
type PutOptions struct {
    // Consistency 一致性级别
    Consistency ConsistencyLevel

    // Sync 是否同步等待确认
    Sync bool
}

// ConsistencyLevel 一致性级别
type ConsistencyLevel int

const (
    // ConsistencyStrong 强一致（Quorum）
    ConsistencyStrong ConsistencyLevel = iota

    // ConsistencyEventual 最终一致（Gossip）
    ConsistencyEventual
)

// PutWithOptions 带选项的写入
func (m *MetadataKV) PutWithOptions(ctx context.Context, ns, key string, value any, opts *PutOptions) error
```

**同步触发**：
```go
// Put 写入后自动触发同步
func (m *MetadataKV) Put(ctx context.Context, ns, key string, value any) error {
    // 1. 写入 MVStore
    if err := m.store.Put(fullKey, data); err != nil {
        return err
    }

    // 2. 根据命名空间选择同步机制
    if requiresStrongConsistency(ns) {
        // 强一致：触发 Quorum 确认
        go m.quorumSync(ns, key, version)
    } else {
        // 最终一致：触发 Gossip 扩散
        go m.gossipSync(ns, key, version)
    }

    return nil
}
```

### 3.3 命名空间到一致性映射

```go
// consistencyMapping 命名空间到一致性级别的映射
var consistencyMapping = map[string]ConsistencyLevel{
    NamespaceCluster:  ConsistencyStrong,  // 集群配置：强一致
    NamespaceNode:     ConsistencyEventual, // 节点信息：最终一致
    NamespaceRole:     ConsistencyEventual, // 角色信息：最终一致
    NamespaceTopo:     ConsistencyEventual, // 拓扑关系：最终一致
    NamespaceShard:    ConsistencyStrong,   // 分片信息：强一致
    NamespaceStatic:   ConsistencyStrong,   // 静态配置：强一致
    NamespaceDynamic:  ConsistencyEventual, // 动态状态：最终一致
    NamespaceOp:       ConsistencyEventual, // 操作记录：最终一致
    NamespaceVersion:  ConsistencyStrong,   // 版本控制：强一致
}

func requiresStrongConsistency(ns string) bool {
    return consistencyMapping[ns] == ConsistencyStrong
}
```

### 3.4 文件结构

```
internal/metadata/
├── kvstore/
│   ├── namespaces.go       ✅ 已创建（9 个命名空间常量）
│   ├── metadata_kv.go      待实现（核心封装 + 一致性路由）
│   ├── codec.go            ✅ 已创建（MessagePack 编解码）
│   └── errors.go           ✅ 已创建（专用错误类型）
├── types/                  待实现（6 种强类型元数据）
└── api/                    待实现（高层 API）
```

---

## 4. 风险评估

| 风险点 | 等级 | 应对措施 |
|--------|------|----------|
| 性能下降 | 中 | MessagePack 高效序列化 + 性能基准测试 |
| 兼容性风险 | 高 | 新架构作为可选层，逐步迁移 |
| 并发安全 | 中 | sync.RWMutex + 并发测试 |
| 测试覆盖不足 | 中 | TDD 开发 + 表格驱动测试 |
| **一致性策略复杂** | 中 | 清晰的命名空间映射 + 默认策略 |

---

## 5. 预审批确认

> **架构师评审**：✅ 评审通过

**评审记录**：
- 第1轮：补充元数据映射说明，包括更新频率和一致性要求
- 第2轮：添加三者关系 Mermaid 图表

**签字/备注**：👤 架构师 2025-02-10 该Feature方案可行，风险可控，同意启动开发，需严格按照文档落地，确保CI通过后提交Post总结。

---

**完整文档**: `docs/06_project_management/pr_documents/feature/2025-02-10_PR-MetadataKV_元数据管理_全流程.md`
