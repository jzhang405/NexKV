# PR 本地验证流程

> **重要**：在推送代码前，必须完成以下本地验证流程

## 完整 PR 流程

```mermaid
flowchart TD
    A[1. 建立新分支 feature/XXX] --> B[2. 编写 Pre 文档]
    B --> C[3. 等待架构师评审 Pre]
    C -->|通过| D[4. 启动开发]
    D --> E[5. 本地验证流程]
    E --> F[6. AI Agent 联合评审]
    F --> G[7. 修复评审问题]
    G --> H[8. 等待架构师评审 Post]
    H -->|通过| I[9. 推送并等待 CI]
    I -->|通过| J[10. 合并到 main]
```

---

## 步骤 5：本地验证流程

### 验证步骤（按顺序执行）

```bash
# 1. 编译项目
make build

# 2. 代码质量检查
make lint

# 3. 格式化代码
make fmt

# 4. 运行测试
make test

# 5. 清理编译文件
make clean
```

### 快速验证命令（一行执行）

```bash
make build && make lint && make fmt && make test && make clean && echo "✅ 所有检查通过"
```

### 验证说明

| 步骤 | 命令 | 说明 |
|------|------|------|
| 1 | `make build` | 编译项目，自动编译 Protobuf 文件 |
| 2 | `make lint` | 代码质量检查（golangci-lint） |
| 3 | `make fmt` | 格式化代码 + go vet 静态检查 |
| 4 | `make test` | 运行所有测试，生成覆盖率报告 |
| 5 | `make clean` | 清理编译文件，保持工作目录整洁 |

---

## 步骤 6：AI Agent 联合评审

> **重要**：在架构师评审前，必须完成 Code Review Agent 和 Code Architect Agent 的联合评审

### 评审流程

```mermaid
flowchart LR
    A[本地验证通过] --> B[Code Review Agent]
    A --> C[Code Architect Agent]
    A --> D[TDD Orchestrator Agent]
    B --> E[生成 Review 报告]
    C --> F[生成架构评审意见]
    D --> G[生成测试覆盖率报告]
    E --> H[汇总评审结果]
    F --> H
    G --> H
    H --> I{有问题?}
    I -->|是| J[修复问题]
    J --> A
    I -->|否| K[提交架构师评审]
```

### 6.1 Code Review Agent 评审

**评审重点**：
- **P0（高风险）**：安全漏洞、DoS 风险、数据损坏
- **P1（中风险）**：性能问题、资源泄漏、并发安全
- **P2（低风险）**：代码风格、注释质量、可读性

**调用方式**：
```
使用 tdd-workflows:code-reviewer agent 进行代码审查
```

### 6.2 Code Architect Agent 评审

**评审重点**：
- **协议兼容性**：变更对现有协议的影响
- **架构合理性**：设计是否合理
- **扩展性**：是否为未来扩展留足空间
- **向后兼容**：对现有客户端/服务端的影响

**调用方式**：
```
使用 data-engineering:backend-architect agent 进行架构评审
```

### 6.3 TDD Orchestrator Agent 评审（测试覆盖率）

**评审重点**：
- **测试覆盖率**：当前测试覆盖情况
- **缺失场景**：未覆盖的边界条件、错误处理
- **测试质量**：测试用例完整性、可维护性
- **改进建议**：新增测试用例建议、优先级排序

**调用方式**：
```
使用 tdd-workflows:tdd-orchestrator agent 进行测试 review 和覆盖率分析
```

### 6.4 联合评审报告格式

```markdown
## 联合评审报告汇总

### Code Review Agent 发现
| 优先级 | 问题 | 建议 |
|--------|------|------|
| P0 | ... | ... |
| P1 | ... | ... |
| P2 | ... | ... |

### Code Architect Agent 发现
| 优先级 | 问题 | 建议 |
|--------|------|------|
| P0 | ... | ... |
| P1 | ... | ... |
| P2 | ... | ... |

### TDD Orchestrator Agent 发现（测试覆盖率）
| 优先级 | 缺失场景 | 建议新增测试 |
|--------|----------|-------------|
| P0 | ... | ... |
| P1 | ... | ... |
| P2 | ... | ... |

### 当前覆盖率
| 模块 | 当前覆盖率 | 目标覆盖率 | 差距 |
|------|-----------|-----------|------|
| Frame/TLV | ~92% | 90%+ | ✅ |
| TCP Transport | ~85% | 90% | 5% |
| UDP Transport | ~88% | 90% | 2% |
| ... | ... | ... | ... |

### 评审结论
- ✅ 通过：无阻塞问题，可提交架构师评审
- ⚠️ 有条件通过：有 P1/P2 问题需修复
- ❌ 不通过：有 P0 问题必须先修复
```

---

## 注意事项

- ⚠️ **所有步骤必须通过后才能提交代码**
- ⚠️ **lint 发现问题时必须修复后再继续**
- ⚠️ **测试失败时必须修复后再继续**
- ⚠️ **联合评审发现的 P0 问题必须修复**
- ⚠️ **P1/P2 问题建议修复，但不阻塞提交**
