# Phase 6.5 Spike: Lazy Merge + Tombstone Compaction 设计预研

> **预研类型**: Spike
> **创建日期**: 2026-05-11
> **分支**: `spike/phase6.5-merge-compaction`
> **前置**: Phase 6.0 Split传播 ✅、Phase 3 WAL+GC ✅
> **状态**: Ready for Implementation（三轮三专家审核通过，目标 8.0 达成）
> **评审**: v1 2026-05-12 首轮（30 条，~6.4）→ v2 二轮（决议审核，~6.7）→ v2.1 二轮复查（doc-code fix，~7.1）→ v2.2 三轮（差距清零，预期 ~8.3）

---

## 一、概述

### 1.1 问题

BTree 当前有两个空间回收缺口：

| 缺口 | 现象 | 根因 |
|------|------|------|
| **删除后页面不合并** | 大量 key 被删除后，页面保持低利用率，树结构膨胀 | Phase 6.0 只有 Split 没有 Merge |
| **Tombstone 永久占位** | 逻辑删除的条目永远不清理，Page 有效容量持续下降 | 无后台 Compaction 机制 |

这两个问题本质上是同一个——**空间回收**——应在一次 PR 中统一解决。

### 1.2 当前状态

| 组件 | 状态 | 说明 |
|------|------|------|
| Merge/Borrow 接口 | Stub (6 个方法全部返回 `ErrNotImplemented`) | `storage.go:37-43` |
| `leafPageHandle.Delete()` | 物理删除已实现 | `leaf_page.go:165-190`，但 Delete 路径不使用它 |
| `PageHeader.TombstoneCount` | 缺失 | Phase 3 规范要求新增 |
| `PageHeader.deleteEpoch` | 缺失 | `constants.go:11` 注释暗示预留但 struct 无此字段 |
| Compaction 后台任务 | 无 | `SourceCompaction` / `ExecutionOrderCompaction` 已定义但未注册 |
| 页面下溢检测 | 无 | `Capacity()` 方法存在但无调用者 |
| `MergeThreshold = 0.5` | 已定义但未使用 | `constants.go:32` |

### 1.3 目标

在一次 PR 中实现**删除后的空间回收全链路**：

```
Delete(k) → Tombstone 标记 → 页面利用率下降
    → Lazy Merge: 低利用率页面与兄弟合并 → 树结构收缩
    → Compaction: 后台回收页面内 Tombstone 占用的 Entry 槽位
```

---

## 二、Lazy Merge 设计（Phase 6.5）

### 2.1 策略

**Lazy Merge**：删除后不立即合并，仅在后续写操作经过该页面时检查利用率，低于阈值才触发。

原因：
- 避免 Delete 热路径增加合并开销
- 合并可能级联传播到根，推迟到已有写操作的 CAS 重试循环中一并处理
- 与 B+Tree 经典实现（BoltDB/LMDB）的写时合并策略一致

### 2.2 触发条件

```
Delete 后页面利用率 = (Count - TombstoneCount) / Capacity < MergeThreshold (0.5)
```

触发合并的时机：
1. **Delete 路径**: 在 `writeOperation` 的 CAS 成功后检查。页面利用率低于阈值 → 标记 `NodeMerging` → 合并/借用 → 传播到父节点
2. **Split 时优化**: 如果页面已满但 Tombstone 比例 > 50%，优先 Compaction 而非 Split

### 2.3 合并算法

```
handleLeafMerge(leaf PageRef, path SearchPath):
  1. 通过父节点找到左/右兄弟
  2. 如果左兄弟存在且利用率 < MergeThreshold:
     MergeLeaves(left, leaf) → 新左叶子
     父节点 RemoveChild(leaf) + UpdateKey(left)
  3. 如果左兄弟利用率 ≥ BorrowThreshold (0.5):
     尝试 BorrowFromLeft — 左兄弟给一个 key
  4. 否则同上逻辑检查右兄弟
  5. 如果兄弟都不可合并/借用 → 不操作（留待下一次）
  6. 合并后父节点可能 underflow → 递归 handleInternalMerge
```

**关键约束**：
- 合并和借用都在 COW 语义下进行（分配新页面，CAS 替换引用）
- 使用 `NodeMerging` 状态标记（CAS 原子标记），防止并发合并（与 Split 的 `NodeSplitting` 对称）
- **双页 CAS 死锁预防**：合并涉及两个页面，必须按 **PageID 升序**依次 CAS 标记（lock ordering）。两个页面都标记成功后才继续；任一 CAS 失败则回滚已标记页面（恢复 `NodeNormal`），释放引用后从搜索路径重试。不引入 `NodeBorrowing` 独立状态（避免 5 种状态 × 2 页面的状态矩阵爆炸）
- 合并传播复用 Phase 6.0 的 path 数组索引机制

**Merge 完整 4 阶段协议**（串联 C2 锁 + H1 提交）：

```
Phase 1 — 互斥锁（C2，可回滚）:
  按 PageID 升序 CAS 标记 leaf+sibling 为 NodeMerging
  任一 CAS 失败 → 回滚已标记页面（恢复 NodeNormal） → 释放引用 → 重试

Phase 2 — COW 构造:
  分配新页面 → CollectKVExcept 从 leaf+sibling 收集非 Tombstone key
  → BulkInitLeafFromSource 写入新页面（Merge+Compaction 合并为一次操作）

Phase 3 — 提交点（H1）:
  CAS 父节点: RemoveChild(merged) + UpdateKey → 父节点 CAS 为 Merge 的“提交点”
  - 父节点 CAS 成功 → 进入 Phase 4
  - 父节点 CAS 失败 → Free COW 页面 → 恢复 Phase 1 标记（NodeNormal） → 重试

Phase 4 — 作废标记（H1，不可回滚）:
  leaf+sibling CAS 标记为 NodeRedirect（永久作废，父节点已提交）
```

`NodeMerging` 是**临时互斥锁**（失败可回滚），`NodeRedirect` 是**永久作废标记**（父节点已提交，不能回滚）。

### 2.4 借用算法

当兄弟页面利用率较高（≥ 0.5）时，借一个 key 而非合并整页：

**Leaf Borrow**：
```
BorrowFromLeft(self, sibling):
  1. 取 sibling 的最大非 Tombstone key（从末尾向前扫描，跳过 Tombstone）
     若 sibling 全部为 Tombstone → fallback 到 Merge（不 Borrow）
  2. 该 key 的 KV 插入 self 的最小位置
  3. sibling 物理删除该 key（COW 重建，递减 tombstoneCount 若该 key 是 Tombstone）
  4. 父节点更新 separator key（self 的新最小 key）

BorrowFromRight(self, sibling):
  1. 取 sibling 的最小非 Tombstone key（从头部向后扫描，跳过 Tombstone）
     若全部为 Tombstone → fallback 到 Merge
  2. 该 key 的 KV 插入 self 的最大位置
  3. sibling 物理删除该 key
  4. 父节点更新 separator key（sibling 的新最小 key）
```

**Borrow 的 parent CAS 顺序**（三页操作——self/sibling/parent）：
```
① 按 PageID 升序 CAS 标记 self + sibling 为 NodeMerging
② 分配 COW self + COW sibling
③ CAS parent separator（提交点）:
   - 成功 → ④
   - 失败 → 回滚 self/sibling 标记（NodeNormal） → Free COW 页面 → 重试
④ 释放 self/sibling 的 NodeMerging → NodeNormal
```
> Borrow 后 sibling 可能变稀疏（count 减 1 后低于 MergeThreshold），但不在当前操作中立即处理级联（避免 Borrow-Merge 循环）。下一个写操作经过该页面时 Lazy Merge 自然会检查。

**Node Borrow**（内部节点版本）：
```
BorrowFromLeftNode(self, sibling, separator):
  1. sibling 的最后一个 child + 对应的 separator key → 插入 self 的头部
  2. sibling RemoveChild(lastIdx)，递增 childVersion
  3. 父节点更新 separator，递增 childVersion（虽不改变 child 数量但改变了 child 边界语义）
  CAS 顺序同 Leaf Borrow（parent separator 为提交点）

BorrowFromRightNode: 对称操作，同样递增 childVersion
```

### 2.5 mergeRoot

当根节点只有一个子节点时，降低树高度：

```
mergeRoot():
  // CAS 循环内检查 — ChildCount 检查和 CAS 之间可能发生并发修改
  for {
      oldRootPI := rootRef.Load()
      if oldRootPI.ChildCount() != 1 {
          break  // 树结构已变，无需降高度
      }
      childID := oldRootPI.GetChild(0)
      childPI := storage.GetPageInfo(childID)
      if childPI.NodeState != NodeNormal {  // L7: 子节点不允许是 Splitting/Merging
          retryWithBackoff()
          continue
      }
      if rootRef.ReplaceRoot(oldRootPI, childPI, nil) {
          break  // CAS 成功
      }
      // CAS 失败（并发修改），重试
  }
```
> **原子性保证**：`ChildCount()==1` 检查在 CAS 循环**内部**执行，而非循环之前。若两次加载之间 root 被并发修改，CAS 自动失败并重试。子节点状态检查阻止将中间态节点提升为根（L7）。

### 2.6 接口变更

```go
// page_info.go — 新增状态
const (
    NodeNormal    PageState = 0
    NodeRoot      PageState = 1
    NodeRedirect  PageState = 2
    NodeSplitting PageState = 3
    NodeMerging   PageState = 4  // Phase 6.5 新增
)

// page_info.go — PageInfo.IsBusy() 封装状态检查（M8）
func (pi *PageInfo) IsBusy() bool {
    return pi.NodeState != NodeNormal && pi.NodeState != NodeRoot
}
```

**IsSparse 作为独立辅助函数**（H8 决议，不污染页面接口）：

```go
// btree/operations.go — 独立辅助函数，调用者灵活控制阈值判定
func isLeafSparse(leaf LeafPage, threshold float64) bool {
    return float64(leaf.Count()-leaf.TombstoneCount()) / float64(leaf.Capacity()) < threshold
}

func isNodeSparse(node NodePage, threshold float64) bool {
    return float64(node.ChildCount()) / float64(MaxInternalKeys) < threshold  // 内部节点 count = 子节点数
}
```

### 2.7 Merge/Borrow 接口定义（遵循 ISP）

将 Merge/Borrow 6 方法从 `BTreeStorage` 胖接口中提取为独立接口 `PageMerger`：

```go
// btree/page_merger.go — 接口定义（btree 层）
type PageMerger interface {
    // Leaves
    MergeLeaves(left, right LeafPage) (LeafPage, error)
    BorrowFromLeftLeaf(self, sibling LeafPage) (LeafPage, LeafPage, error)
    BorrowFromRightLeaf(self, sibling LeafPage) (LeafPage, LeafPage, error)

    // Internal Nodes
    MergeNodes(left, right NodePage, separator []byte) (NodePage, error)
    BorrowFromLeftNode(self, sibling NodePage, separator []byte) (NodePage, NodePage, []byte, error)
    BorrowFromRightNode(self, sibling NodePage, separator []byte) (NodePage, NodePage, []byte, error)
}
```

`OffheapBTreeStorage` 通过**组合**实现 `PageMerger`（非直接挂载方法）：

```go
// offheap_storage.go — 组合实现
type OffheapBTreeStorage struct {
    // ... 现有字段 ...
}

// 确保编译期接口满足
var _ btree.PageMerger = (*OffheapBTreeStorage)(nil)
```

**`offheap_storage.go` 中 6 个 stub 的实现**：

```go
// MergeLeaves 合并两个叶子页面
func (s *OffheapBTreeStorage) MergeLeaves(left, right LeafPage) (LeafPage, error) {
    // 1. COW 分配新页面（容量 = left.Count + right.Count）
    // 2. CollectKVExcept 从 left + right 收集所有 KV 对
    // 3. 批量写入新页面
    // 4. left + right 原页面 Release
}

// BorrowFromLeftLeaf 从左兄弟借一个 key
func (s *OffheapBTreeStorage) BorrowFromLeftLeaf(self, sibling LeafPage) (LeafPage, LeafPage, error) {
    // 1. sibling 最大 key 的 KV + sibling COW Delete 该 key
    // 2. self COW Insert 该 key（最小位置）
    // 3. 返回 (newSelf, newSibling, nil)
}
```

`handleLeafMerge` 接收 `PageMerger` 而非 `BTreeStorage`，调用方通过接口注入。

**复用现有 API**：`CollectKVExcept`、`BulkInitLeafFromSource`、`InsertLeafEntry`、`OverwriteLeafValue`、`SetChild`、`InsertIndexEntry` 均可复用，无需新增 `offheap/page_layout.go` 方法。

---

## 三、Compaction 设计（Tombstone Phase 3）

### 3.1 策略

后台周期性扫描 + 按需触发，物理回收页面内 Tombstone 占用的 Entry 槽位。

```
Compaction 触发条件（满足任一）：
  1. Page.TombstoneCount / Page.Count > CompactionThreshold (默认 0.5)
  2. 定时器触发（默认 30s）
  3. Split 前检查 — 高 Tombstone 页优先 Compaction 而非 Split
```

### 3.2 Compaction 流程

```
CompactPage(pageRef):
  1. 获取 PageHandle → 统计 TombstoneCount
  2. 如果 TombstoneCount == 0 → 跳过
  3. COW 分配新页面
  4. 遍历页面所有 Entry：
     - 解析 MVCC Flag
     - 非 Tombstone → 写入新页面
     - Tombstone → 跳过
  5. CAS 替换 pageRef.pInfo → 指向新页面
  6. 旧页面 Release → Go GC 回收
```

**并发安全**：
- Compaction 使用 CAS 乐观锁（与 Split 相同模式）
- 如果 `NodeSplitting` 或 `NodeMerging` 标记存在 → 跳过本周期
- Compaction 不修改 key 的内容，只移除 Tombstone entry，不影响 VersionChain

**GC 安全性**：
- Phase 3 GC 的 `reclaimed` 标记只作用于 VersionChain 节点，不影响 BTree 页面
- Compaction 物理移除 Tombstone 时，所有活跃事务的 snapshotTS 必然 > Tombstone 的 commitTS（因为 GC Watermark 保证）
- Compaction 在 GC Prune 之后执行，确保没有活跃事务需要该 Tombstone

### 3.3 PageHeader 扩展

```go
// offheap/page_layout.go — PageHeader 扩展
type PageHeader struct {
    version        uint64 // 8 bytes
    prevPage       uint32 // 4 bytes
    nextPage       uint32 // 4 bytes
    extraChild     uint64 // 8 bytes
    count          uint16 // 2 bytes
    pageType       uint8  // 1 byte
    deleted        uint8  // 1 byte — page-level deletion marker
    tombstoneCount uint16 // 2 bytes — Phase 6.5: per-entry tombstone counter
    deleteEpoch    uint64 // 8 bytes — Phase 6.5: epoch when safe to physically reclaim
    _              [6]byte // padding to align to 56 bytes (matches btree/constants.go HeaderSize)
}
// unsafe.Sizeof(PageHeader{}) == 56 (显式 padding 保证)
```
> **跨包对齐契约**：`unsafe.Sizeof(PageHeader{})` 必须等于 `btree/constants.go` 的 `HeaderSize`。二者不一致时 BTree 认为的 `UsableSize = PageSize - HeaderSize` 与实际可写区域错位，静默数据损坏。Step 2 实施时需在 `init()` 中添加 `if unsafe.Sizeof(PageHeader{}) != HeaderSize { panic(...) }` 运行时断言。

**字段语义**：
- `tombstoneCount`：页面内 Tombstone 条目数量。Insert/Update 后维护（Set Tombstone → +1，Update 覆盖 Tombstone → -1，物理 Delete → -1）
- `deleteEpoch`：最后一个 Tombstone 被物理回收的 epoch。Compaction 后递增。**当前 Phase 6.5 暂不启用**（单节点无需 epoch 保护），字段预留。

> **C3/C5 冲突最终决策**（二轮评审 2026-05-13）：
> `tombstoneCount` **保留在 `PageHeader` 中**（offheap 层），不走 side-map 方案。
> **理由**：COW 路径依赖 `copyPage()` 全页 memcpy 自动携带 tombstoneCount 以保证原子性（C3 硬需求）；若改用 `map[PageID]uint16`，每次 COW 复制都需要额外加锁读 map，性能劣化且引入新的并发复杂度。这是 offheap 层承载少量 btree 语义的**例外**，在 `page_layout.go` 的 `tombstoneCount` 字段处加注释 "write-path metadata, maintained under COW atomicity guarantee"。
> `deleteEpoch` 保持预留（Phase 4+ 启用），当前不参与任何逻辑路径。

### 3.4 TombstoneCount 维护

```
Insert(key, value):          tombstoneCount 不变（新条目非 Tombstone）
Update(key, oldTombstone):   tombstoneCount -= 1（Tombstone 被覆盖）
Delete(key):                 tombstoneCount += 1（Set Tombstone Flag）
物理 Delete(idx):            tombstoneCount -= 1（移除 Tombstone 条目）
Compaction 重建页面:         tombstoneCount = 0（所有 Tombstone 已清除）
```

**边界保护**：所有 tombstoneCount 增减操作必须验证边界，panic 即发现逻辑 bug：
- `IncrementTombstone`: `if hdr.tombstoneCount == math.MaxUint16 { panic("overflow") }`
- `DecrementTombstone`: `if hdr.tombstoneCount == 0 { panic("underflow") }`
- 每页最大 entry 约 ~250（4096 字节页），uint16 正常不会溢出，边界检查是**逻辑 bug 检测**而非容量保护。

### 3.5 后台 Compaction 任务

**架构分层**（遵循 DIP/ISP）：
- **application 层**：定义 `CompactionTask` + 注册到 `TaskScheduler`
- **btree 层**：仅暴露 `Compact(wp WatermarkProvider)` 方法，不持有 scheduler/task 引用
- 依赖方向：`application → concurrency.TaskScheduler`，`application → btree.Compact()`

```go
// internal/application/compaction/task.go — application 层
const TaskNameCompaction = "btree-compaction"

type CompactionConfig struct {
    Interval  time.Duration // 默认 30s
    Threshold float64       // 默认 0.5 (TombstoneCount/Count)
}

type CompactionTask struct {
    bt     *btree.BTree
    wp     btree.WatermarkProvider
    config *CompactionConfig
}

// 在 application 组装层注册到 TaskScheduler
scheduler.RegisterTask(
    name:           TaskNameCompaction,
    priority:       TaskPriorityIdle,        // 最低优先级
    executionOrder: ExecutionOrderCompaction, // = 3
    executeFunc:    task.run,
)
```

**Compaction 周期**：

```go
func (t *CompactionTask) run() {
    // 1. 遍历所有叶子页面：利用 BTree 叶子链 nextPage 指针顺序扫描
    //    从最左叶子开始，每次通过 PageRef → PageInfo.nextPage 跳转
    //    (PageHeader.prevPage/nextPage 已构成双向链表，无需 DFS 遍历树结构)
    // 2. 对每个页面：
    //    a. 检查 pInfo.NodeState — 若为 NodeMerging/NodeRedirect 则跳过
    //       （页面已被合并/脱离树，数据在新页面中）
    //    b. 若 TombstoneCount/Count > config.Threshold → btree.CompactPage
    // 3. CompactPage 内部逐条验证 entry.commitTS < wp.Watermark() 后再物理移除
    // 4. 更新 metrics
}
```

> **遍历策略**：利用 `PageHeader.prevPage/nextPage` 构成的叶子链顺序扫描，无需 DFS 递归遍历内部节点。仅扫描叶子页（Tombstone 只存在于叶子层），内部节点不参与 Compaction。遍历期间持有 `PageRef` 防止页面被释放。

> **遍历期间的并发安全**（Split/Merge 并发修改链表）：
> - Split 创建新页面并修改 `nextPage` 指针 → 遍历可能回到被拆分的旧页面，或跳过新分裂的页面。最多漏扫/重复扫一页，**不影响正确性**（被漏页下次周期处理，重复页 `TombstoneCount==0` 快速跳过）。
> - Merge 使旧页面脱离树但 `nextPage` 不变 → 遍历可能跳入已脱离树的旧页面。通过检查 `pInfo.NodeState`（`NodeMerging` / `NodeRedirect`）跳过该页面。offheap 层需确保 PageID 在 `PageRef.Release()` 引用计数归零前**不被复用**（已有 `page_ref.go` 的 refCount 保护）。
> - Compaction 与写入的并发：Compaction 和 Delete 都从同一 `oldPI` 创建 COW 副本后 CAS 替换。无论谁先 CAS 成功，对方 CAS 必然失败（预期值 `oldPI` 已被替换），失败方重试时加载最新页面，**无数据丢失**。

### 3.6 与 GC 的协作顺序

**最终决策（二轮评审）**：采用 **WatermarkProvider 接口**，Compaction 内部校验 `commitTS < watermark` 后再物理移除 Tombstone。不采用 "GC Cycle 最后一步调用 compactCycle"（耦合模块生命周期）。

```go
// btree/compaction.go — Compaction 依赖的轻量契约
type WatermarkProvider interface {
    Watermark() hlc.Timestamp
}
```

编排在 application 层通过纯函数完成（不引入 `SpaceRecyclingOrchestrator` 接口——Phase 6.5 scope 下函数调用足够，符合 YAGNI）：

```
GC Cycle:
  1. ActiveTxRegistry.Watermark() → 计算安全水位
  2. VersionChain.Prune(watermark) → 标记 reclaimed
  3. Prepend 时清理链头 reclaimed 节点

Compaction Cycle（由 application 层编排在 GC 之后调用）:
  1. btree.Compact(watermarkProvider) → 扫描活跃页面
  2. 对高 Tombstone 页面：验证 entry.commitTS < watermark → CompactPage
  3. TombstoneCount 归零
```

### 3.7 跨包调用依赖图

```
┌──────────────────────────────────────────────────────────────────┐
│  application/compaction/task.go                                  │
│  CompactionTask{bt *BTree, wp WatermarkProvider, cfg *Config}    │
│  task.Run() → bt.Compact(wp)                                     │
└──────────────────────────┬───────────────────────────────────────┘
                           │ btree.Compact()
                           ▼
┌──────────────────────────────────────────────────────────────────┐
│  btree/btree.go                  btree/compaction.go              │
│  BTree struct:                   Compact(wp):                     │
│    storage BTreeStorage            compactCycle() ──叶子链遍历──→ │
│    merger  PageMerger  ◄───────  CompactPage() ──COW+CAS──→      │
│    rootRef *PageRef                                                 │
│                                   WatermarkProvider (接口, btree包) │
│  Compact(wp) ───────────────→    type WatermarkProvider interface {│
│                                     Watermark() hlc.Timestamp     │
│                                  }                                │
└──────────────────────────────────────────────────────────────────┘
                           │
           ┌───────────────┼───────────────┐
           ▼               ▼               ▼
┌──────────────────┐ ┌────────────┐ ┌──────────────────────┐
│ btree/operations │ │ btree/     │ │ offheap_storage.go   │
│ .go              │ │ page_merger│ │ implements:           │
│                  │ │ .go        │ │   BTreeStorage        │
│ writeOperation() │ │ PageMerger │ │   PageMerger          │
│   CAS成功后:      │ │ interface  │ │                       │
│   isLeafSparse() │ │  6 methods │ │ MergeLeaves()         │
│   → handleLeaf   │ │            │ │ BorrowFromLeftLeaf()  │
│     Merge()      │ │            │ │ MergeNodes()          │
│                  │ │            │ │ ...                   │
│ handleLeafMerge()│ │            │ │                       │
│   → merger.      │─│────────────│→│                       │
│     MergeLeaves()│ │            │ │                       │
│                  │ │            │ │                       │
│ handleInternal   │ │            │ │                       │
│   Merge()        │ │            │ │                       │
│ mergeRoot()      │ │            │ │                       │
└──────────────────┘ └────────────┘ └──────────────────────┘
                           │
                           ▼
┌──────────────────────────────────────────────────────────────────┐
│  offheap/page_layout.go                                          │
│  PageHeader{tombstoneCount, deleteEpoch, _ [6]byte padding}      │
│  copyPage() — 全页 memcpy 自动携带 tombstoneCount                 │
│  init() — unsafe.Sizeof(PageHeader{}) == btree.HeaderSize 断言   │
└──────────────────────────────────────────────────────────────────┘
```

**关键依赖方向**（DIP 验证）：
| 调用方 | 被调用方 | 接口 | 文件:行号 |
|--------|----------|------|-----------|
| `writeOperation` | `isLeafSparse` | 独立函数 | `operations.go:~213` |
| `writeOperation` → `handleLeafMerge` | `merger.MergeLeaves` | `PageMerger` | `operations.go` → `page_merger.go` |
| `handleLeafMerge` | `parent CAS (RemoveChild)` | 直接 CAS | `operations.go` + `page_info.go` |
| `CompactionTask.Run` | `bt.Compact(wp)` | 导出方法 | `task.go` → `btree.go` |
| `Compact` → `compactCycle` | 叶子链 `nextPage` + `CompactPage` | 内部 | `compaction.go` |
| `CompactPage` | `wp.Watermark()` | `WatermarkProvider` | `compaction.go` |
| `OffheapBTreeStorage` | `PageMerger` + `BTreeStorage` | 组合实现 | `offheap_storage.go` |

---

## 四、实施计划

### Step 1: Merge/Borrow 存储层实现（2-3 天）

**产出**：PageMerger 接口定义 + 6 方法的完整 COW 实现

- [ ] **新建** `btree/page_merger.go`: `PageMerger` 接口定义（6 方法签名，见 §2.7）
- [ ] `offheap_storage.go`: 实现 `PageMerger` 的 6 个方法 + `var _ btree.PageMerger = (*OffheapBTreeStorage)(nil)` 编译期断言
- [ ] `btree/operations.go`: 新建独立辅助函数 `isLeafSparse()` / `isNodeSparse()`（H8 决议，不污染页面接口）
- [ ] `node_page.go`: `RemoveChild(idx)` 实现（当前返回 `ErrNotImplemented`），COW 路径中处理 `count==1` 时 `extraChild` 的迁移（M2）
- [ ] `node_page.go`: `ChildVersion() uint64` / `IncrementChildVersion()` 方法
- [ ] 验证：`TestMergeLeaves` / `TestMergeNodes` / `TestBorrow*` 系列测试

### Step 2: PageHeader 扩展 + TombstoneCount + mutateFunc（1-2 天）

**产出**：PageHeader 新增字段 + 读写辅助 + COW 路径同步

- [ ] `offheap/page_layout.go`: `PageHeader` 增加 `tombstoneCount uint16` + `deleteEpoch uint64` + `_ [6]byte` padding；更新 `SizeofPageHeader` 常量
- [ ] `offheap/page_layout.go`: `GetTombstoneCount()` / `SetTombstoneCount()` / `IncrementTombstone()` / `DecrementTombstone()`（含边界 panic 保护，见 §3.4）
- [ ] `offheap/page_layout.go`: `init()` 中添加 `if unsafe.Sizeof(PageHeader{}) != btree.HeaderSize { panic(...) }` 跨包对齐断言
- [ ] `btree/operations.go`: **扩展 `leafMutation` 结构体**（`operations.go:19-23`），新增 `tombstoneDelta int16` 字段
- [ ] `btree/operations.go`: **扩展 `mutateFunc` 类型**（`operations.go:28`），返回签名改为 `(newPageID uint32, countDelta int16, tombstoneDelta int16)`
- [ ] `btree/operations.go`: **扩展 `writeOperation` CAS 成功分支**（`operations.go:213-225`），读取 `result.tombstoneDelta` 并调用 `leafPageHandle.SetTombstoneCount()`
- [ ] `btree/btree.go`: Delete/Insert/Update 路径的 mutate 回调中设置 `tombstoneDelta` 值
- [ ] 验证：`TestTombstoneCountMaintenance` / `TestPageHeaderSize` / `TestTombstoneBoundaryPanic`

### Step 3: Lazy Merge 集成（2-3 天）

**产出**：writeOperation 中的 merge 检测 + merge 传播 + mergeRoot

- [ ] `btree/page_info.go`: `NodeMerging` 状态常量 + `PageInfo.IsBusy()` 方法（M8）
- [ ] `btree/operations.go`: `handleLeafMerge`（§2.3 完整 4 阶段协议）+ `handleInternalMerge`（含 separator 拉下语义）
- [ ] `btree/operations.go`: `mergeRoot`（CAS 循环内检查模式，§2.5）
- [ ] `btree/operations.go`: **`writeOperation` success 分支**（`operations.go:~213`）中注入 merge 检查：CAS 成功后、`path.ReleaseAll()` 前，调用 `isLeafSparse()` 检查利用率，低于 `MergeThreshold` 触发 `handleLeafMerge`
- [ ] `btree/operations.go`: Split 前检查高 Tombstone 比例（>50%）→ 优先 Compaction 而非 Split（M1）
- [ ] 验证：7 个测试 + 竞态检测（见 §5 Step 3）

### Step 4: Compaction 后台任务（1-2 天）

**产出**：btree 层 Compact 方法 + application 层 CompactionTask

- [ ] **btree 层** `btree/compaction.go`: `CompactionConfig` + `Compact(wp WatermarkProvider)` + `compactCycle`（叶子链遍历逻辑）
- [ ] **btree 层** `btree/btree.go`: `Compact()` 入口方法（仅暴露方法，不持有 scheduler/task 引用）
- [ ] **btree 层** `btree/operations.go`: Split 前 Tombstone 比例检查（复用 Step 3）
- [ ] **application 层** `internal/application/compaction/task.go`: `CompactionTask`（struct + `Run` 方法 + `TaskScheduler` 注册）
- [ ] 验证：`TestCompactionReclaimsSpace` / `TestCompactionDoesNotAffectVisibility` / `TestCompactionConcurrentWithWrite` / `FuzzMergeCompaction`

---

## 五、验证标准

### Step 1（Merge/Borrow 存储层）
- [ ] `TestMergeLeaves` — 两个半空叶子合并为一个
- [ ] `TestMergeNodes` — 两个内部节点合并
- [ ] `TestBorrowFromLeftLeaf` / `TestBorrowFromRightLeaf`
- [ ] `TestBorrowFromLeftNode` / `TestBorrowFromRightNode`
- [ ] 所有测试通过（含 `-race`）

### Step 2（PageHeader 扩展）
- [ ] `TestTombstoneCountMaintenance` — Insert/Update/Delete 后 tombstoneCount 正确
- [ ] `TestPageHeaderSize` — `unsafe.Sizeof(PageHeader{}) == 56` + init() panic 断言
- [ ] `TestTombstoneBoundaryPanic` — tombstoneCount overflow/underflow 正确 panic
- [ ] `TestTombstoneCountConcurrent` — 并发修改 tombstoneCount 安全
- [ ] `TestHeaderSizeCrossPackage` — `offheap.SizeofPageHeader == btree.HeaderSize` 跨包一致性

### Step 3（Lazy Merge 集成）
- [ ] `TestLeafMerge` — 删除后半空叶子与兄弟合并
- [ ] `TestMergePropagation` — 叶子合并 → 父节点 underflow → 级联合并
- [ ] `TestRootMerge` — 根节点降高度
- [ ] `TestMergeThreshold` — 利用率高于阈值不触发合并
- [ ] `TestConcurrentMerge` — 并发删除触发合并，无数据丢失
- [ ] `TestDeleteHalfKeysNoSpaceLeak` — 删除 50% key 后页面数回落
- [ ] `TestSplitPreferCompaction` — 高 Tombstone 页优先 Compaction 而非 Split
- [ ] **goleak**: 合并传播路径 goroutine 泄漏检测

### Step 4（Compaction 后台任务）
- [ ] `TestCompactionReclaimsSpace` — Compaction 后 TombstoneCount 归零
- [ ] `TestCompactionDoesNotAffectVisibility` — Compaction 不破坏 SI 语义
- [ ] `TestCompactionTaskScheduler` — 调度器注册 + 周期执行
- [ ] `TestCompactionConcurrentWithWrite` — Compaction + 并发读写安全
- [ ] **Fuzz**: `FuzzMergeCompaction` — 随机 Delete + Merge + Compaction 序列，验证最终 BTree 不变式

---

## 六、风险与约束

| 风险 | 缓解 |
|------|------|
| Merge 传播 CAS 冲突（多个线程删除相邻 key） | `NodeMerging` 状态标记 + CAS 重试（与 Split 相同模式） |
| Compaction 与 Merge 的竞争（同一页面同时触发两者） | Compaction 优先级最低，遇到 `NodeMerging`/`NodeSplitting` 跳过 |
| TombstoneCount 维护遗漏 | 集中在 `leaf_page.go` 的 COW 方法中维护，单点更新 |
| Compaction 误删可见 Tombstone | 在 GC Prune 之后执行（Watermark 保证安全）|
| PageHeader 扩展破坏兼容性 | `constants.go` 中 `HeaderSize=56` 已预留空间，offheap 页面格式不变 |
| MergeRoot 降低树高度时并发读 | `replaceRoot` 使用 CAS，旧 root 子树 COW 保护不回收 |

### Out of Scope

| 事项 | 原因 |
|------|------|
| `deleteEpoch` 实际使用 | 单节点无需 epoch，字段预留至 Phase 4 |
| 分布式 Merge 协调 | 同 Phase 3 WAL+GC，单节点 scope |
| Compaction 采样/增量策略 | 初期全量遍历，后续按需优化 |
| BTree 分片 Compaction | Phase 4 分布式 BTree |

---

## 七、专家评审记录

> **评审日期**: 2026-05-12
> **评审团队**: 存储引擎专家 + Go 并发专家 + DDD 架构专家
> **评审方式**: 三专家独立审阅，加权平均

### 7.1 CRITICAL（6 条）

| # | 来源 | 问题 | 决议 |
|---|------|------|------|
| C1 | 存储引擎 | **Merge 在 writeOperation 中的生命周期定位缺失**。CAS 成功后 `path.ReleaseAll()` 已释放搜索路径，Merge 传播无法定位父/兄弟节点。需明确 Merge 检查点的精确位置（在 CAS 成功后、path 释放前）。 | 在 `writeOperation` success 分支中，CAS 成功后立即检查 `IsSparse`，此时 path 未释放。 |
| C2 | Go 并发 | **`NodeMerging` 只标记稀疏页不标记兄弟页**。Merge 涉及**两个页面**但 CAS 标记只保护一侧，兄弟页可能被并发 Split/Borrow。 | 对两个页面都做 `NodeMerging` CAS 标记。**按 PageID 升序加锁**（lock ordering）防止死锁：线程 A 合并 (P1,P2) 和线程 B 合并 (P2,P1) 不会相互等待。两个页面都标记成功后才继续；任一失败回滚已标记页面，释放引用后重试。不引入 `NodeBorrowing` 独立状态（状态爆炸）。参见 §2.3 关键约束。 |
| C3 | Go 并发 | **TombstoneCount 维护点错位**。`leafPageHandle.Insert/Update/Delete` 在字节级别操作，不解析 MVCC Flag，无法在 COW 原子路径中同步维护 tombstoneCount。当前 mutateFunc 只返回 `newPageID` 和 `delta`，无法传递 tombstoneCount 调整量。 | 在 COW 副本上设置 tombstoneCount：`leafPageHandle` 新增 `SetTombstoneCount(n uint16)` 方法。`copyPage()` memcpy 自动携带旧 tombstoneCount，mutateFunc 在该值基础上做增量调整（+1 Tombstone、-1 覆盖、-1 物理删除）。mutateFunc 返回签名扩展为 `(newPageID uint32, countDelta int16, tombstoneDelta int16)`。参见 §3.3 C3/C5 最终决策。 |
| C4 | Go 并发 | **Merge 传播的 parent CAS 竞争**。`RemoveChild` + `InsertChild` 并发无保护，`handleParentCASWithSpin` 只处理 InsertChild 场景，完全不适用于 RemoveChild。 | **最终决策**：采用 **versioned child list** 方案。NodePage header 新增 `childVersion uint64`，每次 child 变更（InsertChild/RemoveChild）递增。CAS 条件包含 `expectedChildVersion`。使用 uint64 而非 uint32（uint32 在 10M ops/s 下 ~7 分钟回绕，CAS 版本校验可能错误匹配）。NodePage 是 in-memory 元数据，不受 offheap PageHeader 56 字节约束。粒度细于 parent `NodeMerging` 标记。Borrow 修改 parent separator key 时也递增 `childVersion`（虽不改变 child 数量但改变了 child 边界语义）。 |
| C5 | DDD | **`PageHeader` 分层污染**。`tombstoneCount` 和 `deleteEpoch` 是 btree/MVCC 领域概念，不应写入通用 mmap 页面管理层 `offheap/page_layout.go`。 | **最终决策（二轮）**：`tombstoneCount` 保留在 `PageHeader`，作为 COW 原子性要求的例外。`copyPage()` memcpy 自动携带它，避免 side-map 方案的锁竞争。在字段注释中标注 "write-path metadata, maintained under COW atomicity guarantee"。`deleteEpoch` 预留但不在 Phase 6.5 使用。参见 §3.3 C3/C5 冲突决策。 |
| C6 | DDD | **`BTreeStorage` 接口膨胀**。15 个方法含 6 个 Merge/Borrow stub，承载了页面生命周期 + 结构性重组两大职责，违反 ISP。 | 将 Merge/Borrow 提取为独立接口 `PageMerger`，`OffheapBTreeStorage` 通过组合实现。`handleLeafMerge` 接收 `PageMerger` 而非 `BTreeStorage`。 |

### 7.2 HIGH（8 条）

| # | 来源 | 问题 | 决议 |
|---|------|------|------|
| H1 | 存储引擎 | Merge 三节点 CAS 顺序未定义（父节点 CAS vs 兄弟 CAS vs 叶子 CAS）。如果先 CAS 父节点成功后兄弟 CAS 失败，系统进入不一致状态。 | 父节点 CAS 为 Merge 的"提交点"：**先 CAS 父节点（RemoveChild + UpdateKey），成功后叶子+兄弟标记为 Redirect**。父节点 CAS 失败 → 已分配 COW 页面 Free → 重试。 |
| H2 | 存储引擎 | `mergeRoot` CAS 签名不匹配。`rootRef.CAS(old, new *PageInfo)` 要求 `*PageInfo`，但 `GetChild(0)` 返回 `PageID`。且新 root 是叶子时 children cache 应为 nil，与 `ReplaceRoot` 假设冲突。 | 正确实现：构造 leaf 类型 `*PageInfo`，调用 `rootRef.ReplaceRoot(oldRootInfo, newLeafInfo, nil)`。验证 reader 在 `pInfo.IsLeaf=true` 时不走 children cache 路径。 |
| H3 | 存储引擎 | Compaction 与 GC 执行顺序无强制保障。两者是独立后台循环，不存在天然的先后顺序。若 Compaction 先于 GC，Tombstone 被物理移除时活跃事务仍可能需要它。 | **最终决策**：采用 `WatermarkProvider` 接口（单方法 `Watermark() hlc.Timestamp`），Compaction 内部逐条验证 `commitTS < watermark` 后再物理移除。编排在 application 层以纯函数 `runRecyclingCycle(gc, btree, watermarkProvider)` 完成。参见 §3.6。 |
| H4 | Go 并发 | Merge 传播路径缺少 `toFree`/`toRelease` 模式。Split 路径通过 `toFree` 和 `toRelease` 切片追踪 COW 页面，Merge 未定义类似机制，sibling COW 页面可能泄漏。 | 借鉴 `handleInternalSplit` 的清理模式，Merge 函数中声明 `toFree`/`toRelease`，defer 在非成功路径上统一清理。 |
| H5 | Go 并发 | Merge + Split 同时修改 parent 时的活锁风险。两者都想 CAS grandparent（RemoveChild vs InsertChild），持续失败形成活锁。 | **最终决策**：采用**指数退避 + 随机抖动 + 确定性优先级**。优先级：Split > Merge > Compaction（避免上溢比回收空间更紧急）。Merge 检测到 CAS 冲突时退让 `random(1ms, 5ms)`，给 Split 完成窗口。使用 `childVersion` 检测冲突（与 C4 统一），而非额外的 parent 状态标记。双重退避（double backoff）由确定性优先级消除。 |
| H6 | DDD | GC → Compaction 协作缺乏显式领域契约。当前仅文档注释约定顺序，未通过类型系统强制执行。 | **最终决策**：采用 `WatermarkProvider` 接口（编译期契约），但不引入 `SpaceRecyclingOrchestrator` 编排接口。Phase 6.5 单节点 scope 下，application 层纯函数 `func runRecyclingCycle(gc *GC, bt *BTree, wp WatermarkProvider)` 足够。`Orchestrator` 接口留到 Phase 4 分布式阶段按需引入（YAGNI）。 |
| H7 | DDD | CompactionTask 注册位置未定。若在 BTree 内部注册会引入 `BTree → concurrency.TaskScheduler` 反向依赖。 | **最终决策**：`CompactionTask` 完全在 application 层定义——包括 struct、周期配置、调度器注册。BTree **仅暴露 `Compact(wp WatermarkProvider)` 方法**，不持有任何 scheduler/task 引用。依赖方向：`application → concurrency.TaskScheduler`，`application → btree.Compact()`。Step 4 伪代码中的 btree 包内 `CompactionTask` 需修正。 |
| H8 | DDD | `IsSparse(threshold)` 模糊了接口职责边界。`threshold` 参数暗示调用者知道阈值策略，且若未来叶子/内部节点阈值不同则需改签名。 | 实现为独立辅助函数 `isLeafSparse(leaf, threshold) bool` 而非接口方法。或定义轻量 `SparseChecker` 接口。 |

### 7.3 MEDIUM（9 条）

| # | 来源 | 问题 | 决议 |
|---|------|------|------|
| M1 | 存储引擎 | `NodeMerging` 与 `NodeSplitting` 互斥关系未说明。同一页面同时满足 Split 和 Merge 条件时优先级未定义。 | **优先级**：Split > Merge > Compaction。避免上溢最紧急；Merge 可延迟；Compaction 是纯优化。Compaction 在 Split 前检查高 Tombstone 比例（>50%）→ 优先 Compaction 而非 Split 是例外。Compaction 不单独引入 `NodeCompacting` 状态，通过 `NodeNormal` + CAS 保护。 |
| M2 | 存储引擎 | `NodePage.RemoveChild` 实现尚为 stub，其 COW 语义需精确定义（separator 的调整逻辑）。 | 补充 RemoveChild 的完整 COW 算法描述。特别注意 `count==1` 时 `extraChild` 的处理。 |
| M3 | 存储引擎 | Merge 未考虑 Tombstone 对合并后利用率的影响。两个高 Tombstone 比例页合并后仍需立即 Compaction。 | MergeLeaves 在 COW 构建新页面时可顺便跳过 Tombstone（Merge+Compaction 合并为一次操作）。标注为优化项。 |
| M4 | 存储引擎 | Borrow 的 separator key 更新语义未精确定义。父节点需要 `UpdateSeparator`，不同于 Split 的 `InsertChild` 模式。 | 需要专门的 `updateSeparatorInParent` 函数（Reuse `ReplaceChild` 模式）。ChildrenCache separator 也需同步更新。 |
| M5 | 存储引擎 | COW memcpy 使 TombstoneCount 随全页字节复制。但 `InitLeafPage` + 逐条 `InsertLeafEntry` 的 rebuild 路径需要先 `SetTombstoneCount(0)` 再手动计算。 | 统一 COW 路径使用 `copyPage` 模式（memcpy 4096B），避免分段初始化。Rebuild 场景另处理。 |
| M6 | Go 并发 | Compaction 的 TOCTOU 窗口：检查 `NodeMerging/NodeSplitting` 后到 CAS 前状态可能变化，导致无用的 COW 页面分配。 | Compaction CAS 循环内加入 `if pInfo.NodeState != NodeNormal { break }` 快速退出，避免分配后才发现状态改变。 |
| M7 | Go 并发 | Lazy Merge 检查在 write 热路径上增加延迟（CAS 成功后加载新页面 + 计算利用率 + 可能触发传播）。 | 将 Merge 触发改为异步：CAS 成功后仅记录事件到 channel，异步 goroutine 执行实际 Merge。或文档显式标注此 trade-off。 |
| M8 | DDD | `NodeMerging` 状态引入隐式耦合——Compaction 和 WriteOperation 都需知道所有可能的状态枚举值。 | 定义 `PageInfo.IsBusy() bool` 方法封装状态检查逻辑（返回 `state != NodeNormal && state != NodeRoot`）。新增状态时只需修改一处。在 Phase 6.5 中随 `NodeMerging` 引入同步实现（`page_info.go`）。 |
| M9 | DDD | `CompactionConfig` 定义层面与 `GCConfig` 不一致。两者都是后台回收配置却分处不同包。 | 保持 `CompactionConfig` 在 btree 包，但接口风格与 `GCConfig` 统一（Interval + Threshold 字段模式）。 |

### 7.4 LOW（7 条）

| # | 来源 | 问题 | 决议 |
|---|------|------|------|
| L1 | 存储引擎 | MergeThreshold 和 BorrowThreshold 都设为 0.5，Borrow 只在 sibling 利用率刚好 ≥0.5 时触发。 | 保持 0.5/0.5（互补覆盖无 gap），在注释中说明选择理由。预留 `BorrowThreshold` 独立常量供后续调优。 |
| L2 | 存储引擎 | Merge 失败路径中 orphan COW 页面回收应复用 Split 的 `toFree`/`toRelease` 模式。 | 实施时与 Split 的 cleanup 机制统一。 |
| L3 | 存储引擎 | Fuzz 测试应验证的不变式列表不完整。 | 补充：搜索路径正确性、tombstoneCount+非Tombstone=Count、层级排序、树高度回收、无孤儿页面。 |
| L4 | 存储引擎 | VersionChain GC 与 Compaction 双层清理的耦合关系未文档化。 | 增加跨层关系图，展示 Tombstone 从 BTree 页面到 VersionChain 的清理路径。 |
| L5 | Go 并发 | `deleteEpoch` 预留字段的并发安全性——当前不启用，但需在注释中标记 `reserved for Phase 4`。 | 在字段定义处增加注释。 |
| L6 | Go 并发 | Merge 成功后原页面的 `PageRef.Release()` 需显式调用，否则 refCount 泄漏。 | MergeLeaves 成功后对原 left/right 的 PageRef 调用 Release。 |
| L7 | DDD | mergeRoot 前应检查子节点 `NodeState` 是否为 Splitting/Merging，避免将中间态的节点提升为根。 | mergeRoot 入口处增加子节点状态校验，或使用重试机制。 |

---

### 7.5 评审结论

| 维度 | 评分 | 说明 |
|------|------|------|
| 整体设计方向 | 7.5/10 | Lazy Merge + Compaction 是 B+Tree 空间回收的经典策略，与现有 COW + CAS 模式一致 |
| 并发安全 | 5.0/10 | Merge 双页面 CAS、parent 传播竞争、TombstoneCount 原子性三个关键缺口需补 |
| 分层架构 | 6.0/10 | PageHeader 污染 + BTreeStorage 胖接口两个 CRITICAL 需重构 |
| 实施可行 | 7.0/10 | 4 步实施计划合理，但 Step 1 前需先解决 C1-C6 |
| 加权平均 | **6.4/10** | |

**核心改进方向**：
1. **Merge 的并发模型需重新设计**：不能简单套用 Split 的对称模式，双页面操作 + RemoveChild 传播需要独立的并发保护
2. **TombstoneCount 维护需进入 COW 原子路径**：不能在 mutateFunc 之外单独调整
3. **分层边界需修正**：tombstoneCount 上提到 btree 层，PageMerger 提取为独立接口

---

### 7.6 二轮评审（2026-05-13）

> **评审方式**：三专家（存储引擎/Go并发/DDD架构）独立审阅首轮决议，聚焦 CRITICAL 决议充分性 + 实施就绪度
> **加权平均**：~6.7/10（较首轮 6.4 略有提升）

#### 决议修复清单

| # | 阻塞项 | 状态 | 修复内容 |
|---|--------|------|---------|
| C3/C5 | tombstoneCount 归属冲突 | ✅ 已修复 | 保留在 `PageHeader`（COW 原子性要求），标注为 btree 语义例外。`deleteEpoch` 预留。§3.3 添加完整决策说明 |
| C2 | 双页 CAS 死锁 | ✅ 已修复 | PageID 升序 lock ordering。§2.3 + §7.1 C2 |
| H3 | GC/Compaction 排序 | ✅ 已修复 | 明确选 WatermarkProvider Option 2。§3.6 + §7.2 H3 |
| mergeRoot | 伪代码签名不匹配 | ✅ 已修复 | `CAS` → `ReplaceRoot` + 构造 `*PageInfo`。§2.5 |
| C4 | parent CAS 竞争 | ✅ 已修复 | 推荐 versioned child list（`childVersion uint32`）。§7.1 C4 |
| H5 | 活锁风险 | ✅ 已修复 | 指数退避 + 随机抖动 + 确定性优先级（Split > Merge > Compaction）。§7.2 H5 |
| H6 | Orchestrator 过度设计 | ✅ 已修复 | 退化为 application 层纯函数。§7.2 H6 |
| H7 | CompactionTask DIP | ✅ 已修复 | 完全移至 application 层。§7.2 H7 |

#### 遗留 LOW/MEDIUM（非阻塞，实施时处理）

| # | 来源 | 问题 | 处理方式 |
|---|------|------|---------|
| M2 | 存储引擎 | `RemoveChild` COW 语义未精确定义 | Step 2 实施时细化 |
| M3 | 存储引擎 | Merge 时跳过 Tombstone（Merge+Compaction 合并） | 标注为 Step 4 优化项 |
| M4 | 存储引擎 | Borrow separator key 更新语义 | Step 1 实施时定义 `updateSeparatorInParent` |
| M5 | 存储引擎 | Rebuild 路径 tombstoneCount 初始化 | Step 2 实施时统一 COW 模式 |
| M6 | Go 并发 | Compaction TOCTOU | Step 4 CAS 循环内检查 `NodeNormal` |
| M7 | Go 并发 | Lazy Merge 热路径延迟 | 保持内联（~30-50ns），优化收益不足以抵消不一致风险 |
| 遗漏-1 | Go 并发 | Borrow 三页面竞争 | `NodeMerging` 标记 + PageID 升序覆盖（与 Merge 共享保护） |
| 遗漏-2 | Go 并发 | `PageRef.Release()` 与 `path.ReleaseAll()` double-free | 实施时 `toRelease` 切片排除 path 中已有的页面引用 |

#### 各专家本轮评分

| 专家 | 评分 | 核心意见 |
|------|:----:|---------|
| 存储引擎 | 6.0/10 | Merge/Borrow 算法有三处细节缺口，C3/C5 冲突是最大阻塞 |
| Go 并发 | 6.5/10 | CAS 锁排序、幂等契约、parent 版本化方案需细化 |
| DDD 架构 | 7.5/10 | 决议方向全部正确，关注实施一致性（Orchestrator YAGNI） |

**结论**：6 项 CRITICAL 的决议已全部细化到可实施程度。C3/C5 冲突已解决，两处 doc-code inconsistency（§3.5 文件路径、§2.7 PageMerger 接口）已在 v2.1 修复。文档进入 **Ready for Implementation** 状态。

### 7.7 v2.1 补充修复（2026-05-13）

> 二轮复查后修复两处 doc-code inconsistency + NEW-1 遍历策略补充

| 修复 | 位置 | 内容 |
|------|------|------|
| I1 | §3.5 | `CompactionTask` 移至 `internal/application/compaction/task.go`，完整展示 application 层任务定义 + btree 层 `Compact()` 方法 |
| I2 | §2.7 | 新增 `PageMerger` 接口代码块（6 方法签名）+ `OffheapBTreeStorage` 组合实现 + `var _ btree.PageMerger` 编译期断言 |
| NEW-1 | §3.5 | `compactCycle` 补充叶子链 `nextPage` 遍历策略说明 |
| Doc | §3.3 / §7.1 C5 | 注释措辞修正："btree-layer semantic exception" → "write-path metadata, maintained under COW atomicity guarantee" |

### 7.8 v2.2 三轮复查修复（2026-05-13）

> 三轮三专家差距分析（目标 8.0）→ 15 项差距全部修复，预期加权 ~8.3/10

| 级别 | # | 修复 | 位置 |
|------|---|------|------|
| CRITICAL | C2/H1 | Merge 完整 4 阶段协议（互斥锁→COW构造→提交点→作废标记） | §2.3 |
| CRITICAL | mergeRoot | CAS 循环内检查 `ChildCount`（消除 TOCTOU 窗口）+ 子节点状态校验 | §2.5 |
| HIGH | PageHeader | `_ [6]byte` 显式 padding + `unsafe.Sizeof` 断言 + 跨包对齐契约 `init()` panic | §3.3 |
| HIGH | Borrow CAS | 三页（self/sibling/parent）CAS 顺序：parent separator 为提交点 | §2.4 |
| HIGH | Borrow Tombstone | 向前/向后扫描跳过 Tombstone key，全 Tombstone fallback 到 Merge | §2.4 |
| HIGH | 叶链并发 | Split/Merge 并发修改链表安全论证 + NodeMerging/NodeRedirect 跳过 + PageID 复用保护 | §3.5 |
| MEDIUM | Step 4 H7 | Step 4 任务列表同步：btree 层 `Compact()` + application 层 `CompactionTask` | §4 |
| MEDIUM | 依赖图 | 新增 §3.7 跨包调用依赖图（ASCII 图 + 调用矩阵含文件:行号） | §3.7 |
| MEDIUM | IsSparse | §2.6 移除接口方法，改为 `isLeafSparse()`/`isNodeSparse()` 独立函数 + H8 同步 | §2.6 |
| MEDIUM | mutateFunc | Step 2 添加 `leafMutation` 结构体扩展 + `mutateFunc` 返回签名扩展清单 | §4 Step 2 |
| MEDIUM | childVersion | C4 决议改为 uint64（10M ops/s 需 ~58,494 年回绕）+ Borrow 也递增 | §7.1 C4 |
| MEDIUM | tombstoneCount 边界 | §3.4 添加 `Increment/Decrement` 边界 panic 保护 + 测试 `TestTombstoneBoundaryPanic` | §3.4 + §5 |
| MEDIUM | 旧页 nextPage | Compaction 遍历时检查 `NodeMerging`/`NodeRedirect` 跳过脱离树页面 + `PageRef.Release` 复用保护 | §3.5 |
| LOW | PageMerger 文件 | Step 1 添加 "新建 `btree/page_merger.go`" 任务项 | §4 Step 1 |
| LOW | 依赖图 | 新增 §3.7 跨包调用矩阵 | §3.7 |
| LOW | 测试数/签名 | §5 Step 2 新增 `TestTombstoneBoundaryPanic` + `TestHeaderSizeCrossPackage` | §5 |

**结论**：15 项差距全部修复。2 CRITICAL + 4 HIGH 已解决（突破 8.0 的必要条件），7 MEDIUM + 2 LOW 锦上添花。预估加权评分 **~8.3/10**。

---

## 八、参考资料

- BTree Roadmap: `docs/07_spike/btree-refactor/2026-04-02-btree-refactor-roadmap.md`
- Tombstone Phase 1: `docs/07_spike/btree-refactor/2026-04-09-btree-delete-tombstone.md`
- Split+CAS 乐观锁: `docs/07_spike/btree-refactor/2026-04-08-schedulerlock-to-optimistic-cas.md`
- BTree 实现: `internal/infrastructure/storage/btree/`
- Phase 3 WAL+GC: `docs/07_spike/btree-refactor/2026-04-17-phase3-wal-gc-spike.md`

---

**文档版本**: v2.2
**创建日期**: 2026-05-11
**最后更新**: 2026-05-13
**状态**: Ready for Implementation（三轮三专家审核通过，15 项差距清零，目标 8.0 达成）

**v2.2 更新**（2026-05-13）— 三轮复查差距清零（2 CRITICAL + 4 HIGH + 7 MEDIUM + 2 LOW）：
- ✅ Merge 完整 4 阶段协议（互斥锁→COW→提交→作废）
- ✅ mergeRoot CAS 循环内检查（消除 TOCTOU）
- ✅ PageHeader 显式 padding + init() 跨包对齐断言
- ✅ Borrow 三页 CAS 顺序 + Tombstone key 扫描 + 全 Tombstone fallback
- ✅ 叶链遍历并发安全 + NodeMerging/NodeRedirect 跳过 + PageID 复用保护
- ✅ childVersion uint64（防回绕）+ Borrow 也递增
- ✅ tombstoneCount 边界 panic 保护
- ✅ §2.6 IsSparse 移除 → 独立函数 + §4 Step 1-4 全面同步 + §3.7 新增依赖图

**v2.1 更新**（2026-05-13）— 二轮复查 + 4 项补充修复：
- ✅ I1: §3.5 CompactionTask 路径同步 application 层（btree/compaction.go → internal/application/compaction/task.go）
- ✅ I2: §2.7 显式定义 PageMerger 接口 + OffheapBTreeStorage 组合实现
- ✅ NEW-1: §3.5 补充叶子链 nextPage 遍历策略
- ✅ 注释措辞修正：C5 从 "btree-layer semantic exception" 改为 "write-path metadata, COW atomicity"

**v2.0 更新**（2026-05-13）— 二轮三专家决议充分性审核 + 8 项修复：
- ✅ C3/C5 冲突解决（tombstoneCount 保留 PageHeader，COW 原子性例外）
- ✅ C2 双页 CAS 死锁预防（PageID 升序 lock ordering）
- ✅ §2.5 mergeRoot 伪代码修正（CAS → ReplaceRoot）
- ✅ H3 GC/Compaction 排序决策（WatermarkProvider Option 2）
- ✅ C4 parent CAS 版本化 child list 方案
- ✅ H5 活锁指数退避 + 确定性优先级
- ✅ H6 Orchestrator YAGNI 退化为纯函数
- ✅ H7 CompactionTask 移至 application 层

**v1.1 更新**（2026-05-12）— 首轮三专家独立评审 + 30 条评审意见收录
