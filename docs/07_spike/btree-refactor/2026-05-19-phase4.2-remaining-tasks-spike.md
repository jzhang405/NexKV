# Phase 4.2-4.4 剩余任务 + 全局未完成项清单

> 创建日期：2026-05-19
> 前置：Phase 4.1 ChunkManager + PageSerializer（已完成，已合并 main）
> 状态：Planning

---

## 一、Phase 4.2: RestoreDiskChunkManager

> 目标：从已有 `.ao` 文件恢复 ChunkManager 状态（重启/崩溃恢复）
> 对齐：Lealone `ChunkManager.init()` 恢复协议
> 预计文件：`disk_chunk_manager.go`（新增函数）、`chunk_file.go`（readHeader 完善）

### 1.1 恢复协议（8 步）

```
RestoreDiskChunkManager(dir string) (*DiskChunkManager, error)

1. 扫描目录: os.ReadDir → 过滤 btree_*_*.ao → 解析文件名
2. 删除零长度文件（崩溃时创建但未写入）
3. 对每个文件:
   a. 正则 btree_(\d+)_(\d+)\.ao → chunkID + seq
   b. 追踪 maxSeq 及其对应的 chunkID
   c. 处理重复 chunkID: 保留最高 seq 的文件名（备份恢复场景）
4. 按 seq 升序排序所有 Chunk
5. 打开最高 seq 的 Chunk 作为 lastChunk
6. 验证双块头部（readHeader）:
   - 块 0 有效 → 解析头部
   - 块 0 损坏 + 块 1 有效 → 从副本恢复
   - 两块都损坏 → 标记为损坏，跳过（WARNING 日志）
7. 从头部恢复元数据: pageCount, removedPages, pagePosToLen
8. 重建内存结构: chunkIDs BitSet, idToChunk, chunks 切片
```

### 1.2 新增函数

```go
// disk_chunk_manager.go

// RestoreDiskChunkManager 从已有 .ao 文件恢复（重启场景）。
func RestoreDiskChunkManager(dir string) (*DiskChunkManager, error)

// parseChunkFilename 解析 "btree_0_1.ao" → (chunkID=0, seq=1)
func parseChunkFilename(name string) (chunkID uint32, seq uint64, err error)

// recoverFromHeader 从 header 恢复 chunk 的元数据映射
func (cm *DiskChunkManager) recoverFromHeader(c *ChunkFile, h *ChunkHeader) error
```

### 1.3 需要修改的现有代码

| 文件 | 变更 |
|------|------|
| `chunk_file.go:readHeader` | 移除 `//nolint:unused`，完善错误路径 |
| `chunk_header.go:parse*` | 将 TODO 注释中的静默吞错改为返回 error（或保留 TODO 待 Phase 4.2 统一修改） |
| `chunk_file.go:openChunkFile` | 支持以只读方式打开已有文件（当前只支持 O_RDWR\|O_CREAT\|O_EXCL） |

### 1.4 验证标准

- `RestoreDiskChunkManager` 能在空目录下创建首个 chunk（等价于 `NewDiskChunkManager`）
- 能从有 chunk 文件的目录恢复，idToChunk、chunks 状态正确
- 双块故障转移：块 0 损坏时能从块 1 恢复
- `go test -race` 通过，覆盖率 >= 80%

---

## 二、Phase 4.3: WAL-AO 集成

> 目标：Checkpoint 时将 mmap 脏页序列化到 AO 文件，建立 pageID→ChunkPosition 映射
> 前置：Phase 4.2 完成

### 2.1 集成点

```
Checkpoint 时序（Fuzzy Checkpoint）:

T1: WAL 记录当前 LSN (checkpointStartLSN)
T2: BTree.EnumeratePages(rootRef) 后序遍历
     ├─ 子页面排在父页面之前（Children-First 语义）
     └─ 输出 []PageFlushItem
T3: 对每个 PageFlushItem:
     ├─ ChunkPos == 0（脏页）→ cm.Allocate + cm.WritePage
     └─ ChunkPos != 0（已持久化）→ 跳过
T4: cm.Sync() — 所有 chunk 文件 fsync
T4a: pageLocs 批量更新（AO Sync 后、WAL Append 前）
T5: WAL.Append(CheckpointEntry) — checkpointStartLSN + rootPageID + pageLocs
T6: WAL.Sync()
T7: WAL.Truncate(checkpointStartLSN) — 丢弃已持久化的旧 WAL
```

### 2.2 需要修改的代码

#### (A) PageInfo + PageHeader 添加 ChunkPos

```go
// internal/infrastructure/storage/offheap/page_layout.go

// PageInfo 新增字段
type PageInfo struct {
    // ... existing fields ...
    ChunkPos model.ChunkPosition // AO 位置（0=脏页，对齐 Lealone pos==0 语义）
}

// PageHeader 新增字段（辅助校验，非权威）
// chunkPos uint64 使用原 [16]byte 填充的一半，剩余 [8]byte
```

#### (B) OffheapBTreeStorage 适配

```go
// btree/offheap_storage.go

type OffheapBTreeStorage struct {
    pm         *offheap.PageManager
    cm         service.ChunkManager   // Phase 4 新增
    serializer *chunk.PageSerializer  // Phase 4 新增
    pageLocs   sync.Map               // map[PageID]ChunkPosition
}

// GetLeafPage 增加惰性加载路径:
//   1. 从 PageManager 获取
//   2. 未命中 → 从 pageLocs 查找 ChunkPosition → cm.ReadPage → Deserialize
```

#### (C) BTreeScanner 接口扩展

```go
// checkpoint/checkpoint_manager.go

type BTreeScanner interface {
    RootPage() PageRef
    // EnumeratePages 后序遍历所有可达 PageRef
    EnumeratePages(root PageRef) ([]PageFlushItem, error)
}

type PageFlushItem struct {
    PageID   model.PageID
    PageType uint8
    PageData []byte             // 预序列化数据；nil = 已持久化
    ChunkPos model.ChunkPosition // 0 = 脏页
}
```

#### (D) CheckpointManager 集成

```go
type Manager struct {
    wal        service.WAL
    btree      BTreeScanner
    cm         service.ChunkManager  // Phase 4 新增
    serializer *chunk.PageSerializer // Phase 4 新增
}

func (m *Manager) FuzzyCheckpoint() error
```

### 2.3 惰性加载路径

```
读取流程:
  GetLeafPage(pageID):
    1. page := pm.PageIDToPtr(pageID)
    2. if page is valid → return page  // 已在 mmap 缓存中
    3. pos, ok := pageLocs.Load(pageID)
    4. if !ok → return ErrPageNotFound
    5. rawPageID := pm.Alloc()          // 分配物理 mmap 页
    6. data := cm.ReadPage(pos)
    7. serializer.Deserialize(data, pm.PageIDToPtr(rawPageID))
    8. return page  // 惰性加载完成
```

### 2.4 验证标准

- `FuzzyCheckpoint` 能将脏页写入 AO，pageLocs 映射正确
- 惰性加载：从未缓存的 pageID 可正确从 AO 恢复
- `go test -race` 通过，覆盖率 >= 80%

---

## 三、Phase 4.4: GC 集成

> 目标：WAL GC 与 Chunk 页回收联动
> 前置：Phase 4.3 完成

### 3.1 联动协议

```
WAL GC 触发条件（任一）:
  - Checkpoint 完成后自动触发 Truncate
  - 手动触发 / 定时 GC

GC 流程:
  1. FuzzyCheckpoint() 完成后 → cm.Sync() → WAL.Truncate(checkpointStartLSN)
  2. ChunkCompactor 异步运行:
     a. 收集所有 removedPages
     b. 计算每个 Chunk 的 fillRate
     c. 重写低填充率 Chunk 的活跃页面到新 Chunk
     d. 更新 PageRef 的 ChunkPos
     e. 删除旧 Chunk 文件
```

### 3.2 ChunkCompactor 实现（Phase 5 完整版）

当前 `chunk_compactor.go` 为 stub（`NeedCompaction()→false`, `Compact()→nil`）。Phase 4.4 需要：

| 函数 | 状态 | Phase 4.4 动作 |
|------|------|---------------|
| `NeedCompaction()` | stub | 实现 fillRate 检查 |
| `Compact()` | stub | 实现 Lealone 压缩算法（贪心选择+重写） |
| `executeCompact()` | 未定义 | 新增：主入口 |
| `getFillRate()` | 未定义 | 新增：填充率计算 `1 + 98*live/total` |

### 3.3 removedPages 全局化

Phase 4.1 中 `removedPages` 存储在 `ChunkFile` 级别（per-chunk map）。GC 需要全局视图：

```go
// 方案 A: 全局 removedPages 集合（对齐 Lealone ConcurrentSkipListSet<Long>）
type DiskChunkManager struct {
    // ...
    removedPages map[model.ChunkPosition]struct{} // 全局已删除页面集
}
```

### 3.4 验证标准

- Checkpoint 后 WAL Truncate 正确执行（旧 WAL 文件被清理）
- ChunkCompactor 能识别低填充率 Chunk
- 压缩后活跃页面在新 Chunk 中可读
- `go test -race` 通过，覆盖率 >= 80%

---

## 四、全局未完成项清单

> 基于 `docs/07_spike/btree-refactor/` 中所有 spike 文档的交叉分析

### 4.1 Phase 4 WAL+AO（本次重点）

| 子任务 | 状态 | 优先级 |
|--------|------|--------|
| Phase 4.1: ChunkManager + PageSerializer | ✅ 完成 | — |
| Phase 4.2: RestoreDiskChunkManager | ⬜ 待开始 | P0 |
| Phase 4.3: WAL-AO 集成（Checkpoint + 惰性加载） | ⬜ 待开始 | P0 |
| Phase 4.4: GC 集成（WAL GC + ChunkCompactor） | ⬜ 待开始 | P1 |

### 4.2 BTree Delete + Tombstone

| 来源文档 | 状态 | 
|----------|------|
| `2026-04-09-btree-delete-tombstone.md` | 🔄 进行中 |
| `btree/compaction.go` | 部分实现 |
| `btree/merge.go` | 部分实现 |
| `btree/merge_ops.go` | 部分实现 |

**剩余工作**：
- Delete 操作的 Tombstone 标记完整性
- Tombstone 驱动的惰性 Merge 触发条件
- Tombstone GC 与 Compaction 的联动

### 4.3 Phase 6.5 Lazy Merge + Tombstone Compaction

| 来源文档 | 状态 |
|----------|------|
| `2026-05-11-phase6.5-merge-compaction-spike.md` | Ready for Implementation（三专家审核通过） |

**剩余工作**：
- `btree/compaction.go:Compaction` 从 stub 到完整实现
- `btree/merge.go:MergeLeaves` / `MergeNodes` 完整实现
- `ChunkCompactor.executeCompact`（与 Phase 4.4 共享）

### 4.4 MVCC Phase 2

| 来源文档 | 状态 |
|----------|------|
| `2026-04-10-mvcc-phase2-plan.md` | 🔄 进行中 |

**已实现**（`mvcc/` 22 个文件）：
- Transaction（Begin/Commit/Rollback）
- VersionChain（版本链）
- GC（垃圾回收）
- KeyLock（键级锁）
- TSCodec/TSGenerator

**剩余工作**：
- MVCC 与 WAL Recovery 的集成（事务恢复）
- VersionChain GC 与 Checkpoint 的协调
- 快照隔离级别的冲突检测完善

### 4.5 Phase 3 WAL+GC

| 来源文档 | 状态 |
|----------|------|
| `2026-04-17-phase3-wal-gc-spike.md` | Draft（十轮审核完成 v2.8） |

**已实现**（`wal/` 13 个文件 + `checkpoint/` 5 个文件）：
- DiskWAL（Segment 轮转 + CRC32C + Group Commit）
- Recovery（WAL 扫描 + 重放）
- CheckpointManager（FuzzyCheckpoint 基础）

**剩余工作**：
- WAL GC 与 AO Checkpoint 的 Truncate 联动（Phase 4.4）
- VersionChain 持久化到 WAL
- Checkpoint 中断的 Rollback 安全性

---

## 五、建议执行顺序

```
Phase 4.2  ← 当前分支 feature/phase4.2-restore-chunk-manager
  ↓
Phase 4.3  (WAL-AO 集成)
  ↓
Phase 4.4  (GC 集成)
  ↓
Phase 6.5  (Lazy Merge + Tombstone Compaction)
  ↓
BTree Delete Tombstone 补全
  ↓
MVCC Phase 2 补全
```

Phase 4.2-4.4 为 P0 级（恢复路径是 KV 存储的基础安全保证），Phase 6.5 为 P1（性能优化），MVCC Phase 2 为 P1（事务功能增强）。

---

## 六、参考

- 详细设计：`2026-05-16-phase4-wal-ao-persistence-spike.md`（1649 行，§3 接口设计 + §4 文件格式 + §6 集成点）
- Lealone 对齐：`2026-04-01-lealone-btree-deep-dive.md`（ChunkManager.init 恢复协议）
- 路线图：`2026-04-02-btree-refactor-roadmap.md`
- WAL 规范：`2026-04-17-phase3-wal-gc-spike.md`
- Phase 6.5 规范：`2026-05-11-phase6.5-merge-compaction-spike.md`

---

**文档版本**：v1.0
**状态**：Planning
