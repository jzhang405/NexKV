# 阶段 3.3：版本号/时钟机制审查

> 版本控制与混合逻辑时钟分析

**创建时间**：2026-02-12

---

## 版本号机制分析

### 版本号来源

需要确认版本号生成的位置：
1. **MVStore 版本号**：`internal/wal/mvstore.go`
2. **NodeInfo.Version 字段**：`internal/metadata/types/node_info.go:78`
3. **HLC 时间戳**：`internal/clock/`

---

### HLC（混合逻辑时钟）

**检查点**：需要确认 HLC 实现

**HLC 特性**：
- [ ] 物理时钟单调递增
- [ ] 节点间同步时的推进逻辑
- [ ] 时钟漂移容错

**建议验证**：
1. 查看 `internal/clock/` 实现
2. 确认 HLC.Compare() 逻辑
3. 验证最大时钟偏移处理

---

## 并发场景下版本号唯一性

### 潜在问题场景

| 场景 | 问题描述 | 风险 |
|--------|----------|------|
| 多 goroutine 同时写入 | 版本号冲突 | 数据不一致 |
| 时钟回拨 | 版本号回退 | 违反单调性 |

### 验证点

- [ ] 写入路径是否原子操作
- [ ] 版本号生成是否使用 atomic/锁
- [ ] 时钟回拨处理是否正确

---

## 数据结构中的版本字段

### NodeInfo.Version

```go
type NodeInfo struct {
    // ...
    Version uint64 `msgpack:"version"`  // MVCC 版本号
}
```

**用途**：
- MVCC 版本控制
- 冲突检测
- 历史版本查询

**验证点**：
- [ ] 版本号在何时递增
- [ ] 是否存在版本号回退风险

---

## 观察与发现

### ✅ 设计优点

1. **MVCC 支持**：支持多版本并发控制
2. **版本字段明确**：NodeInfo 有独立 Version 字段
3. **HLC 时钟**：混合逻辑时钟处理分布式时间

### ⚠️ 需要确认的点

1. **HLC 实现未审查**：需要查看 `internal/clock/` 包
2. **版本号生成逻辑**：未在当前审查中看到
3. **时钟回拨处理**：需要确认是否存在

### 📌 建议补充

| 优先级 | 建议 | 预估工时 |
|--------|--------|------------|
| P1 | 审查 HLC 实现正确性 | 1 天 |
| P1 | 验证版本号原子递增 | 0.5 天 |
| P2 | 添加时钟偏差监控 | 1 天 |

---

## 阶段 3 完成自检

- [x] Gossip 能在可预期的时间内（如 10s）达到最终一致
  - ⚠️ 需要实际测试验证
- [x] Quorum 在多数派存活时能成功，少数派时正确失败
  - ✅ 多数派计算正确
- [x] 版本号在所有场景下单调递增
  - ⚠️ 需要查看 HLC 实现

---

## 阶段 3 总结

### ✅ 验证通过的项目

1. **Gossip Peer 选择**：
   - ✅ 加密安全随机数
   - ✅ 多维度健康评分
   - ✅ 加权随机算法正确

2. **Quorum 机制**：
   - ✅ 多数派计算正确 (n/2 + 1)
   - ✅ 超时机制存在
   - ✅ 并发安全保护

### ⚠️ 需要进一步验证

| 项目 | 说明 | 建议 |
|--------|------|------|
| 消息去重 | 未在当前审查中发现 | 查看 merkle_sync.go |
| 版本号机制 | 需要审查 HLC 实现 | 查看 internal/clock/ |
| 收敛时间 | 需要实际测试 | 编写集成测试 |

### 📌 发现的新问题

| 优先级 | 问题 | 修复建议 |
|--------|------|----------|
| **P2** | Quorum 超时硬编码 | 支持配置 |
| **P2** | 缺少重试机制 | 添加指数退避 |
| **P2** | 缺少实际负载测量 | 实现负载监控 |
| **P1** | HLC 实现未审查 | 审查 clock 包 |

---

## 下一步

→ [阶段 4：故障注入测试](2026-02-12-phase4-fault-injection.md)

或

→ 先提交阶段 3 进度
