# 阶段 3：一致性协议验证（核心价值）

> 验证 Gossip 和 Quorum 机制的实现是否符合设计预期。

**预计时间**：4-6 小时
**状态**：⏳ 待开始

---

## 📋 任务清单

### Step 3.1：Gossip 协议正确性验证（2h）

**任务**：
1. [ ] 找到 Gossip 实现的核心代码
2. [ ] 验证关键逻辑的正确性
3. [ ] 编写验证测试用例

**检查点**：

| 检查项 | 验证方法 | 预期结果 | 检查结果 |
|--------|----------|----------|----------|
| 节点选择随机性 | 检查随机数生成 | 均匀分布 | [ ] |
| 版本向量单调性 | 检查版本号生成 | 严格递增 | [ ] |
| 消息去重机制 | 检查消息 ID 记录 | 无重复广播 | [ ] |
| 收敛时间 | 3 节点网络测试 | < 10s | [ ] |

**命令参考**：
```bash
# 找到 Gossip 实现
find internal/ -name "*gossip*" -type f

# 检查随机数生成
grep -r "rand\." internal/gossip/

# 检查版本号生成
grep -r "version\|Version" internal/gossip/
```

#### 测试用例模板

```go
// 3 节点网络，节点 A 发起更新，验证 B 和 C 最终能收到
func TestGossipConvergence(t *testing.T) {
    // 1. 启动 3 个节点
    // 2. 在节点 A 上发起状态更新
    // 3. 验证节点 B 和 C 最终收到更新
    // 4. 测量收敛时间
}
```

---

### Step 3.2：Quorum 机制验证（2h）

**任务**：
1. [ ] 找到 Quorum 计算逻辑
2. [ ] 验证多数派计算的正确性
3. [ ] 测试边界场景

**检查点**：

| 检查项 | 验证方法 | 预期结果 | 检查结果 |
|--------|----------|----------|----------|
| 多数派计算 | 检查公式 N/2 + 1 | 正确 | [ ] |
| 超时处理 | 模拟网络延迟 | 不永久阻塞 | [ ] |
| 失败重试 | 检查退避策略 | 指数退避 | [ ] |
| 少数派拒绝 | 5 节点杀 3 个 | 正确失败 | [ ] |

**测试场景**：

```bash
# 场景 1：5 节点集群，2 节点宕机 → Quorum 成功
# 场景 2：5 节点集群，3 节点宕机 → Quorum 失败
# 场景 3：网络分区 → 多数派侧继续工作
```

**命令参考**：
```bash
# 找到 Quorum 计算
grep -rn "quorum\|Quorum" internal/ --include="*.go"

# 检查超时配置
grep -rn "timeout\|Timeout" internal/ --include="*.go"
```

---

### Step 3.3：版本号/时钟机制审查（1h）

**任务**：
1. [ ] 检查版本号生成逻辑
2. [ ] 验证单调性保证
3. [ ] 检查时钟漂移处理

**检查点**：

| 检查项 | 说明 | 检查结果 |
|--------|------|----------|
| 版本号回退 | 是否存在递减情况 | [ ] |
| 并发唯一性 | 多节点同时生成是否唯一 | [ ] |
| 时钟漂移 | NTP 调整时的处理 | [ ] |
| Lamport 时钟 | 如果使用，是否正确实现 | [ ] |

**命令参考**：
```bash
# 检查时间戳生成
grep -rn "time\.Now\|Unix\|Nano" internal/ --include="*.go"

# 检查版本号比较逻辑
grep -rn "Compare.*Version\|version.*>=" internal/ --include="*.go"
```

---

## 📝 验证报告模板

```markdown
## Gossip 协议验证结果

### 功能验证
- [x] 节点选择随机
- [x] 版本向量单调递增
- [x] 消息去重工作正常

### 性能验证
- 3 节点收敛时间：8.5s ✅
- 5 节点收敛时间：12.3s ✅
- 10 节点收敛时间：18.7s ⚠️ (略慢)

### 问题记录
1. 问题：收敛时间随节点数增长较快
   影响：大规模集群可能需要调整
   建议：增加 fanout 参数或使用 hybrid Gossip

---

## Quorum 机制验证结果

### 功能验证
- [x] 多数派计算正确 (N/2 + 1)
- [x] 超时机制有效
- [x] 少数派正确拒绝

### 边界测试
- [x] 2/5 节点宕机：Quorum 成功
- [x] 3/5 节点宕机：Quorum 失败
- [x] 网络分区：多数派继续工作

### 问题记录
无重大问题
```

---

## ✅ 完成自检

- [ ] Gossip 能在可预期的时间内（如 10s）达到最终一致
- [ ] Quorum 在多数派存活时能成功，少数派时正确失败
- [ ] 版本号在所有场景下单调递增

---

## 📌 本阶段产出文件

- `phase3_gossip_verification.md` - Gossip 验证报告
- `phase3_quorum_verification.md` - Quorum 验证报告
- `phase3_versioning_audit.md` - 版本号机制审查
