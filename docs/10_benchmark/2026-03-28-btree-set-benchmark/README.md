# BTree Set Baseline 性能测试 (2026-03-28)

## 测试环境

| 项目 | 值 |
|------|-----|
| CPU | 12 核 |
| OS | Linux 6.17.0-14-generic |
| Go | 1.26.0 linux/amd64 |
| 分支 | `perf/btree-set-benchmark` (基于 main 6fef0da) |
| 工具 | `cmd/btree_perf_scheduler` (builtin 模式) |

## 测试结果

### Test 1: GOGC=500, 50K ops/thread, init=1000

| 并发度 | 总操作数 | 吞吐量(ops/s) | 平均延迟(μs) | 扩展比 |
|--------|---------|--------------|-------------|--------|
| 1 | 50,000 | 16,816 | 59.47 | 1.00x |
| 2 | 100,000 | 11,117 | 89.95 | 0.66x |
| 4 | 200,000 | 14,212 | 70.36 | 0.85x |
| 8 | 400,000 | 4,378 | 228.42 | 0.26x |

### Test 2: GOGC=500, 10K ops/thread, init=1000

| 并发度 | 总操作数 | 吞吐量(ops/s) | 平均延迟(μs) | 扩展比 |
|--------|---------|--------------|-------------|--------|
| 1 | 10,000 | 231,066 | 4.33 | 1.00x |
| 2 | 20,000 | 50,478 | 19.81 | 0.22x |
| 4 | 40,000 | 30,772 | 32.50 | 0.13x |
| 8 | 80,000 | 18,475 | 54.13 | 0.08x |

### Test 3: GOGC=off, 50K ops/thread, init=1000

| 并发度 | 总操作数 | 吞吐量(ops/s) | 平均延迟(μs) | 扩展比 |
|--------|---------|--------------|-------------|--------|
| 1 | 50,000 | 14,706 | 68.00 | 1.00x |
| 2 | 100,000 | 9,279 | 107.77 | 0.63x |
| 4 | 200,000 | 4,480 | 223.19 | 0.30x |
| 8 | 400,000 | 2,962 | 337.63 | 0.20x |

## 关键发现

### 1. 负扩展比 — 并发反而更慢
所有测试均呈现**负扩展**：增加线程数吞吐量下降。8 线程性能仅为单线程的 8%~26%。

### 2. 数据量敏感 — 树越大越慢
- 10K ops 单线程: **231K ops/s** (4.33 μs)
- 50K ops 单线程: **16.8K ops/s** (59.47 μs) — 慢 14x

树从 1K 条增长到 51K 条时，延迟从 4μs 涨到 59μs。可能原因：
- 叶子分裂频率随数据量增加
- 树高度增加导致 search path 变长
- 物化（materialize）开销随数据量增加

### 3. GOGC=off 反而更慢
GOGC=off 时性能比 GOGC=500 更差（14.7K vs 16.8K 单线程），说明瓶颈不在 GC，而在锁竞争和树操作本身。

### 4. 与历史数据对比
memory 中的历史基准（GOGC=500, builtin 模式）：
| 并发度 | 历史 | 当前 | 变化 |
|--------|------|------|------|
| 1 | 557K ops/s | 231K ops/s (10K ops) | -58% |
| 8 | 1,648K ops/s | 18K ops/s (10K ops) | -99% |

当前性能远低于历史基准，可能是近期代码修改引入了性能退化。

## 瓶颈假设

1. **CAS 重试风暴**：`setDirect` 最多重试 10 次，并发时大量操作失败后丢弃（successCount 远低于 totalOps）
2. **PageLock 竞争**：TryLock 失败直接返回 ErrRetry，高并发下大量重试
3. **物化开销**：`materialize()` 占总分配 26.43%（pprof 数据），每次写入可能触发物化
4. **叶子分裂**：分裂操作持有锁时间长，阻塞其他写入

## 下一步

- [ ] 添加 success rate 指标（successCount / totalOps）
- [ ] pprof CPU profiling 确认热点
- [ ] 对比 `main` 分支上次高性能版本定位退化
- [ ] 分析 10K vs 50K 的性能悬崖点

## 优化记录

### Phase 0: DebugPrintf 清空 (2026-03-28)

将 `btree.DebugPrintf` 和 `offheap.DebugPrintf` 函数体清空为 no-op，消除运行时分支判断开销。

**测试条件**: GOGC=500, 10K ops/thread, init=200

| 并发度 | 基线 (50K ops, init=1000) | Phase 0 (10K ops, init=200) | 备注 |
|--------|--------------------------|----------------------------|------|
| 1 | 16,816 ops/s (59.47μs) | **118,959 ops/s (8.41μs)** | ops 数量不同，不可直接对比 |
| 2 | 11,117 ops/s (89.95μs) | **353,448 ops/s (2.83μs)** | ops 数量不同，不可直接对比 |
| 4 | 14,212 ops/s (70.36μs) | **35,511 ops/s (28.16μs)** | ops 数量不同，不可直接对比 |
| 8 | 4,378 ops/s (228.42μs) | **19,703 ops/s (50.75μs)** | ops 数量不同，不可直接对比 |

**说明**: ops 数量和 init 数据量不同，不能直接对比绝对值。但 1T/2T 的延迟从 μs 级降到 sub-10μs 级别，说明小数据集下性能良好。4T/8T 仍然存在负扩展性，需要后续优化锁竞争和 CAS 重试。

## 详细数据

- [builtin-gogc500-50k.md](./builtin-gogc500-50k.md) — GOGC=500, 50K ops (基线)
- [builtin-gogc500-10k.md](./builtin-gogc500-10k.md) — GOGC=500, 10K ops
- [builtin-gogcoff-50k.md](./builtin-gogcoff-50k.md) — GOGC=off, 50K ops
- [pprof-analysis.md](./pprof-analysis.md) — CPU profiling 分析
