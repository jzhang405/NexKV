{
  "project": "NexKV",
  "branchName": "ralph/transport-layer-code-review",
  "description": "Transport Layer Code Review and Bug Fixes - Fix P0 critical bugs in UDP bitmap overflow, MultiTransport duplicate sends, and RPC message validation",
  "userStories": [
    {
      "id": "US-001",
      "title": "Fix UDP bitmap overflow when total=64",
      "description": "As a developer, I need to fix the integer overflow bug in UDP fragment reassembly when total fragments equals 64, to prevent incomplete data from being marked as complete.",
      "acceptanceCriteria": [
        "Add special case check: if p.total == 64, return p.bitmapFast == 0xFFFFFFFFFFFFFFFF",
        "Otherwise compute mask as uint64(1)<<p.total - 1",
        "Fix located in udp_transport.go isComplete() method around line 509",
        "Add unit test TestUDPFragmentBitmap_Total64 that creates 64 fragments and verifies completeness check",
        "Add unit test TestUDPFragmentBitmap_Total65 to verify it still works for >64",
        "Unit tests written for bitmap overflow fix",
        "All tests pass (go test ./internal/metadata/transport/...)",
        "Test coverage >80% for new code",
        "Build passes (go build ./...)",
        "golangci-lint passes with no errors"
      ],
      "priority": 1,
      "passes": true,
      "notes": "COMPLETED: Fixed bitmap overflow bug, added 3 unit tests, all verification passed"
    },
    {
      "id": "US-002",
      "title": "Fix MultiTransport duplicate send in select statement",
      "description": "As a developer, I need to remove the duplicate case in MultiTransport's select statement to prevent logic confusion and potential message loss.",
      "acceptanceCriteria": [
        "Remove duplicate 'case mt.overflowCh <- msgFrame:' at line 396 in multi_transport.go",
        "Keep only single case for overflowCh with proper logging",
        "Add default case that logs warning when both channels are full",
        "Update logging to include message type and protocol type",
        "Add unit test TestMultiTransportOverflowFull that fills both channels and verifies behavior",
        "Unit tests written for duplicate send fix",
        "All tests pass (go test ./internal/metadata/transport/...)",
        "Test coverage >80% for new code",
        "Build passes (go build ./...)",
        "golangci-lint passes with no errors"
      ],
      "priority": 2,
      "passes": true,
      "notes": "COMPLETED: Fixed duplicate select case, added TestMultiTransportOverflowFull, all verification passed"
    },
    {
      "id": "US-003",
      "title": "Add RPC message validation for ExpectResponse enum",
      "description": "As a developer, I need to add validation for the ExpectResponse enum value in RPC message parsing to reject invalid messages early.",
      "acceptanceCriteria": [
        "Add validation: if data[10] > 1, return error 'invalid ExpectResponse value: %d'",
        "Update error message to include detailed field breakdown (MsgSeq=8, IsRequest=1, IsError=1, ExpectResponse=1)",
        "Validation located in rpc_transport.go decodeMessage around line 438",
        "Add unit test TestRPCParseInvalidExpectResponse with values >1",
        "Add unit test TestRPCParseInvalidLength with data length < 11",
        "Unit tests written for RPC validation fix",
        "All tests pass (go test ./internal/metadata/transport/...)",
        "Test coverage >80% for new code",
        "Build passes (go build ./...)",
        "golangci-lint passes with no errors"
      ],
      "priority": 3,
      "passes": true,
      "notes": "COMPLETED: Added ExpectResponse enum validation, 2 unit tests, fixed existing tests compatibility, all verification passed"
    },
    {
      "id": "US-004",
      "title": "Run complete regression test suite (FINAL)",
      "description": "As a developer, I need to verify all P0 bug fixes by running the complete test suite and ensuring no regressions.",
      "acceptanceCriteria": [
        "All existing transport package tests pass",
        "Test coverage maintained at ≥82.3%",
        "golangci-lint returns 0 issues",
        "No performance regression in benchmark tests",
        "Code Reviewer Agent validates all fixes",
        "Full test suite passes (go test -v -race -cover ./...)",
        "Build succeeds (go build ./...)",
        "Application works end-to-end with all transports"
      ],
      "priority": 4,
      "passes": true,
      "notes": "COMPLETED: All regression tests passed. Build, lint, test all successful. Coverage maintained at 82.3%. No regressions detected."
    }
  ]
}
